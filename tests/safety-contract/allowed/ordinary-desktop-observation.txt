Process.GetProcessesByName("EscapeFromTarkov");
EnumWindows(enumerateVisibleWindow, IntPtr.Zero);
GetWindowRect(gameWindow, out visibleBounds);
graphics.CopyFromScreen(visibleBounds.Left, visibleBounds.Top, 0, 0, size);
HttpClient.GetAsync("https://api.tarkov.dev/graphql");
