<UserControl x:Class="TarkovCompanion.App.Views.V2.Shell.V2ShellView"
             x:DataType="vm:V2ShellViewModel"
             xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:TarkovCompanion.App.ViewModels.V2.Shell"
             xmlns:stashVm="using:TarkovCompanion.App.ViewModels.V2.StashScan"
             xmlns:stashViews="using:TarkovCompanion.App.Views.V2.StashScan"
             xmlns:debriefVm="using:TarkovCompanion.App.ViewModels.V2.Debrief"
             xmlns:debriefViews="using:TarkovCompanion.App.Views.V2.Debrief"
             xmlns:planVm="using:TarkovCompanion.App.ViewModels.V2.Plan"
             xmlns:planViews="using:TarkovCompanion.App.Views.V2.Plan"
             xmlns:teamVm="using:TarkovCompanion.App.ViewModels.V2.Team"
             xmlns:teamViews="using:TarkovCompanion.App.Views.V2.Team"
             xmlns:raidVm="using:TarkovCompanion.App.ViewModels.V2.Raid"
             xmlns:pages="using:TarkovCompanion.App.ViewModels"
             xmlns:intelVm="using:TarkovCompanion.App.ViewModels.V2.Intel"
             xmlns:intelViews="using:TarkovCompanion.App.Views.V2.Intel"
             xmlns:prim="using:TarkovCompanion.App.Views.V2.Primitives">
  <UserControl.DataTemplates>
    <DataTemplate DataType="{x:Type stashVm:StashScanWorkspaceViewModel}">
      <stashViews:StashScanWorkspaceView />
    </DataTemplate>
    <DataTemplate DataType="{x:Type debriefVm:DebriefWorkspaceViewModel}">
      <debriefViews:DebriefWorkspaceView />
    </DataTemplate>
    <!-- V2 rough package 10 (Plan workspace + Hideout section). Refs #288. -->
    <DataTemplate DataType="{x:Type planVm:PlanWorkspaceViewModel}">
      <planViews:PlanWorkspaceView />
    </DataTemplate>
    <DataTemplate DataType="{x:Type planVm:HideoutWorkspaceViewModel}">
      <planViews:HideoutWorkspaceView />
    </DataTemplate>
    <!-- V2 rough package 28 (parity): the reference workspaces beside Intel. -->
    <DataTemplate DataType="{x:Type intelVm:AmmoWorkspaceViewModel}">
      <intelViews:AmmoWorkspaceView />
    </DataTemplate>
    <DataTemplate DataType="{x:Type intelVm:KeysWorkspaceViewModel}">
      <intelViews:KeysWorkspaceView />
    </DataTemplate>
    <DataTemplate DataType="{x:Type intelVm:FleaWorkspaceViewModel}">
      <intelViews:FleaWorkspaceView />
    </DataTemplate>
    <!-- #287: Crafts & barters, a fourth reference workspace beside Ammo/Keys/Flea. -->
    <DataTemplate DataType="{x:Type intelVm:CraftsBartersWorkspaceViewModel}">
      <intelViews:CraftsBartersWorkspaceView />
    </DataTemplate>
    <!-- V2 rough package 25 (#402): the Keep list, a Plan section beside Hideout. -->
    <DataTemplate DataType="{x:Type planVm:KeepListWorkspaceViewModel}">
      <planViews:KeepListWorkspaceView />
    </DataTemplate>
    <DataTemplate DataType="{x:Type pages:LoadoutPageViewModel}">
      <planViews:LoadoutWorkspaceView />
    </DataTemplate>
    <DataTemplate DataType="{x:Type pages:EventsPageViewModel}">
      <planViews:EventsWorkspaceView />
    </DataTemplate>
    <!-- v2r-team (package 9, wave 2): the Team workspace, shared by the Team/Group/Tablet routes. -->
    <DataTemplate DataType="{x:Type teamVm:TeamWorkspaceViewModel}">
      <teamViews:TeamWorkspaceView />
    </DataTemplate>
  </UserControl.DataTemplates>
  <UserControl.Resources>
    <!-- V2 rough package 15: outline rail icons (Themes/V2/V2Icons.axaml), not a font
         glyph. Shared between the rail's destinations and its Setup gear so both draw the same
         way. Purely decorative: AccessibilityView="Raw", and the button around it always carries
         the real accessible Name (Label). -->
    <DataTemplate x:Key="V2RailIconTemplate" x:DataType="vm:V2ShellDestinationViewModel">
      <Panel Width="24" Height="24" AutomationProperties.AccessibilityView="Raw">
        <Path Classes="v2-icon" IsVisible="{Binding IsRaid}" Data="{DynamicResource V2.Icon.Map}" />
        <Path Classes="v2-icon" IsVisible="{Binding IsIntel}" Data="{DynamicResource V2.Icon.Document}" />
        <Path Classes="v2-icon" IsVisible="{Binding IsPlan}" Data="{DynamicResource V2.Icon.Clipboard}" />
        <Path Classes="v2-icon" IsVisible="{Binding IsTeam}" Data="{DynamicResource V2.Icon.People}" />
        <Path Classes="v2-icon" IsVisible="{Binding IsDebrief}" Data="{DynamicResource V2.Icon.Chart}" />
        <Path Classes="v2-icon" IsVisible="{Binding IsSetup}" Data="{DynamicResource V2.Icon.Gear}" />
        <!-- [#294] Something is waiting behind this destination. V1 marked its rail beside
             Settings when a build had arrived and V2 marked nothing at all, so an update reached
             the machine and told nobody who had not opened Setup of their own accord.

             In the shared icon template, so every place a destination is drawn carries it and
             none of them has to remember to. Decorative like the icon it sits on: the words are
             on the button, in NoticeDescription. -->
        <Ellipse Width="8" Height="8" Margin="0,0,1,1"
                 HorizontalAlignment="Right" VerticalAlignment="Bottom"
                 IsVisible="{Binding HasNotice}"
                 Fill="{DynamicResource V2.Brush.Info}"
                 AutomationProperties.AccessibilityView="Raw" />
      </Panel>
    </DataTemplate>
  </UserControl.Resources>
  <UserControl.Styles>
    <!-- Reserve the selected outline in every state. Changing border geometry while a
         destination moves between rows can make WrapPanel and its Grid parent remeasure each
         other indefinitely at an exact wrap boundary. -->
    <Style Selector="Button.v2-destination">
      <Setter Property="BorderBrush" Value="{DynamicResource V2.Brush.Border}" />
      <Setter Property="BorderThickness" Value="2" />
    </Style>
    <Style Selector="Button.v2-destination /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="BorderThickness" Value="{DynamicResource V2.Focus.Indicator.Thickness}" />
    </Style>
    <Style Selector="Button.v2-destination.selected">
      <Setter Property="Background" Value="{DynamicResource V2.Brush.SurfaceRaised}" />
      <Setter Property="BorderBrush" Value="{DynamicResource V2.Brush.Action}" />
    </Style>
    <Style Selector="Border.v2-shell-rail">
      <Setter Property="Background" Value="{DynamicResource V2.Brush.Surface}" />
      <Setter Property="BorderBrush" Value="{DynamicResource V2.Brush.Border}" />
      <Setter Property="BorderThickness" Value="0,0,1,0" />
      <Setter Property="Padding" Value="{DynamicResource V2.Space.Md.Inset}" />
    </Style>
    <Style Selector="Border.v2-shell-dialog-scrim">
      <Setter Property="Background" Value="{DynamicResource V2.Brush.Canvas}" />
      <Setter Property="Opacity" Value="0.98" />
      <Setter Property="Padding" Value="{DynamicResource V2.Space.Xl.Inset}" />
    </Style>
    <Style Selector="Panel.v2-availability-stale > Rectangle.v2-badge-outline">
      <Setter Property="Stroke" Value="{DynamicResource V2.Brush.Warning}" />
    </Style>
    <Style Selector="Panel.v2-availability-partial > Rectangle.v2-badge-outline">
      <Setter Property="Stroke" Value="{DynamicResource V2.Brush.Warning}" />
    </Style>
    <Style Selector="Rectangle.v2-pattern-double">
      <Setter Property="StrokeThickness" Value="4" />
    </Style>

    <!-- V2 rough package 15 (shell chrome + Raid workspace): a single slim top bar, the design
         concepts' left rail, and a compact global-problem banner, replacing the #265 scaffold's
         stacked header/nav/address rows. -->
    <Style Selector="Border.v2-topbar">
      <Setter Property="Background" Value="{DynamicResource V2.Brush.Surface}" />
      <Setter Property="BorderBrush" Value="{DynamicResource V2.Brush.Border}" />
      <Setter Property="BorderThickness" Value="0,0,0,1" />
      <Setter Property="Padding" Value="20,0,12,0" />
      <Setter Property="Height" Value="60" />
    </Style>
    <Style Selector="Rectangle.v2-topbar-sep">
      <Setter Property="Width" Value="1" />
      <Setter Property="Height" Value="28" />
      <Setter Property="Fill" Value="{DynamicResource V2.Brush.SurfaceRaised}" />
      <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
    <Style Selector="ComboBox.v2-topbar-map">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="FontWeight" Value="SemiBold" />
    </Style>
    <Style Selector="Border.v2-topbar-tag">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="Padding" Value="0" />
    </Style>
    <!-- V2 rough package 20: the one global-problem line. A tinted strip the width of the window,
         one row high, with no dashed outline — it must not read as a card. -->
    <Style Selector="Border.v2-surface-banner">
      <Setter Property="Background" Value="{DynamicResource V2.Brush.SurfaceRaised}" />
      <Setter Property="BorderThickness" Value="0,0,0,1" />
      <Setter Property="BorderBrush" Value="{DynamicResource V2.Brush.Border}" />
      <Setter Property="CornerRadius" Value="0" />
      <Setter Property="Padding" Value="20,6" />
    </Style>
    <Style Selector="Border.v2-surface-banner.v2-tone-warning > Grid > TextBlock.v2-glyph">
      <Setter Property="Foreground" Value="{DynamicResource V2.Brush.Warning}" />
    </Style>
    <Style Selector="Border.v2-surface-banner.v2-tone-danger > Grid > TextBlock.v2-glyph">
      <Setter Property="Foreground" Value="{DynamicResource V2.Brush.Danger}" />
    </Style>
    <Style Selector="Border.v2-surface-banner Button.v2-banner-action">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="MinHeight" Value="0" />
      <Setter Property="Padding" Value="10,3" />
    </Style>
    <Style Selector="Border.v2-status-pill">
      <Setter Property="Background" Value="{DynamicResource V2.Brush.SurfaceRaised}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="CornerRadius" Value="{DynamicResource V2.Shape.Control}" />
      <Setter Property="Padding" Value="14,6" />
    </Style>
    <Style Selector="Border.v2-status-pill TextBlock">
      <Setter Property="Foreground" Value="{DynamicResource V2.Brush.Warning}" />
    </Style>
    <Style Selector="Border.v2-status-pill.v2-availability-ready TextBlock">
      <Setter Property="Foreground" Value="{DynamicResource V2.Brush.Success}" />
    </Style>
    <Style Selector="Button.v2-topbar-plain">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Padding" Value="0" />
    </Style>
    <Style Selector="Border.v2-status-pill.v2-availability-ready">
      <Setter Property="BorderBrush" Value="{DynamicResource V2.Brush.Success}" />
    </Style>
    <Style Selector="Border.v2-status-pill:not(.v2-availability-ready)">
      <Setter Property="BorderBrush" Value="{DynamicResource V2.Brush.Warning}" />
    </Style>
    <!-- Secondary in-workspace routes (Loot decision under Raid, Hideout under Plan, ...) read as
         a slim tab strip rather than a bordered button box. -->
    <Style Selector="Border.v2-tabbar">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderBrush" Value="{DynamicResource V2.Brush.Border}" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Padding" Value="0" />
    </Style>
    <Style Selector="Button.v2-tab">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderThickness" Value="0,0,0,2" />
      <Setter Property="BorderBrush" Value="Transparent" />
      <Setter Property="CornerRadius" Value="0" />
      <Setter Property="Padding" Value="4,6" />
      <Setter Property="Margin" Value="0,0,12,0" />
      <Setter Property="Foreground" Value="{DynamicResource V2.Brush.TextSecondary}" />
    </Style>
    <Style Selector="Button.v2-tab.selected">
      <Setter Property="Foreground" Value="{DynamicResource V2.Brush.TextPrimary}" />
    </Style>
    <Style Selector="Button.v2-tab.selected">
      <Setter Property="BorderBrush" Value="{DynamicResource V2.Brush.Action}" />
    </Style>
    <!-- The rail: flat rows, not bordered boxes. Selection is a tinted background plus a left
         accent bar, like docs/design/v2's concept renders. -->
    <Style Selector="Button.v2-rail-item">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderThickness" Value="3,0,0,0" />
      <Setter Property="BorderBrush" Value="Transparent" />
      <Setter Property="CornerRadius" Value="0" />
      <Setter Property="Padding" Value="19,0,12,0" />
      <Setter Property="Height" Value="60" />
      <Setter Property="FontSize" Value="{DynamicResource V2.Type.Body.Size}" />
    </Style>
    <Style Selector="Button.v2-rail-item.selected TextBlock">
      <Setter Property="Foreground" Value="{DynamicResource V2.Brush.Action}" />
    </Style>
    <Style Selector="Button.v2-rail-item.selected Path.v2-icon">
      <Setter Property="Stroke" Value="{DynamicResource V2.Brush.Action}" />
    </Style>
    <Style Selector="Button.v2-rail-item.selected">
      <Setter Property="Background" Value="{DynamicResource V2.Brush.SurfaceRaised}" />
      <Setter Property="BorderBrush" Value="{DynamicResource V2.Brush.Action}" />
    </Style>
    <!-- [V2 rough package 46] The rail's own collapse control: quiet, in the corner it collapses
         towards, and never wider than the collapsed rail. -->
    <Style Selector="Button.v2-rail-collapse">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Padding" Value="6" />
      <Setter Property="CornerRadius" Value="{DynamicResource V2.Shape.Control}" />
      <Setter Property="Opacity" Value="0.7" />
    </Style>
    <Style Selector="Button.v2-rail-collapse:pointerover">
      <Setter Property="Opacity" Value="1" />
    </Style>
    <Style Selector="Border.v2-shell-rail-edge">
      <Setter Property="Width" Value="18" />
      <Setter Property="Background" Value="{DynamicResource V2.Brush.Surface}" />
      <Setter Property="BorderBrush" Value="{DynamicResource V2.Brush.Border}" />
      <Setter Property="BorderThickness" Value="0,0,1,0" />
    </Style>
    <Style Selector="Button.v2-nav-launcher">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Padding" Value="0" />
      <Setter Property="Width" Value="18" />
      <Setter Property="Height" Value="44" />
      <Setter Property="Opacity" Value="0.7" />
      <Setter Property="HorizontalContentAlignment" Value="Center" />
      <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>
    <Style Selector="Button.v2-nav-launcher:pointerover">
      <Setter Property="Opacity" Value="1" />
    </Style>
    <Style Selector="Button.v2-nav-launcher Path.v2-icon">
      <Setter Property="Width" Value="16" />
      <Setter Property="Height" Value="16" />
    </Style>
    <Style Selector="Button.v2-rail-gear">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="HorizontalAlignment" Value="Left" />
      <Setter Property="Margin" Value="12,12,12,16" />
      <Setter Property="Padding" Value="10" />
      <Setter Property="CornerRadius" Value="{DynamicResource V2.Shape.Control}" />
    </Style>
    <Style Selector="Button.v2-rail-gear.selected">
      <Setter Property="Background" Value="{DynamicResource V2.Brush.SurfaceRaised}" />
    </Style>
  </UserControl.Styles>

  <Grid x:Name="ShellRoot"
        Classes="v2-density-standard"
        Classes.v2-density-compact="{Binding UsesCompactDensity}"
        Background="{DynamicResource V2.Brush.Canvas}">
    <!-- Disabling the application layer while the one dialog host is open keeps keyboard focus
         inside that dialog without creating a second window or runtime graph. -->
    <Grid IsEnabled="{Binding HasNoDialog}" RowDefinitions="Auto,Auto,Auto,*">
      <Border Grid.Row="0" Classes="v2-topbar"
              AutomationProperties.AccessibilityView="Control"
              AutomationProperties.ControlTypeOverride="ToolBar"
              AutomationProperties.Name="{Binding AppName}">
        <!-- Left to right like the concepts: name, map, mode, raid clock, data freshness; then
             search/setup/capture and the status pill at the far right. -->
        <Grid ColumnDefinitions="Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,*,Auto,Auto,Auto,Auto,Auto" ColumnSpacing="16">
          <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
            <TextBlock Classes="v2-heading-3" Text="{Binding AppName}" />
            <Border Classes="v2-topbar-tag" VerticalAlignment="Center">
              <TextBlock Classes="v2-label" Text="{Binding AppTag}" />
            </Border>
          </StackPanel>
          <Rectangle Grid.Column="1" Classes="v2-topbar-sep" IsVisible="{Binding ShowsMapSelector}" />
          <ComboBox Grid.Column="2" Classes="v2-topbar-map" MinWidth="120" VerticalAlignment="Center"
                     IsVisible="{Binding ShowsMapSelector}"
                     ItemsSource="{Binding RaidCockpitWorkspace.MapPicker}"
                     SelectedItem="{Binding RaidCockpitWorkspace.SelectedMap, Mode=OneWay}"
                     SelectionChanged="TopBarMapSelectionChanged"
                     AutomationProperties.AutomationId="v2-shell-topbar-map"
                     AutomationProperties.Name="Switch map">
            <ComboBox.ItemTemplate>
              <DataTemplate x:DataType="raidVm:RaidMapPickerItemViewModel">
                <TextBlock Text="{Binding Name}" />
              </DataTemplate>
            </ComboBox.ItemTemplate>
          </ComboBox>
          <Rectangle Grid.Column="3" Classes="v2-topbar-sep" />
          <TextBlock Grid.Column="4" Classes="v2-label" VerticalAlignment="Center"
                     AutomationProperties.AutomationId="v2-shell-topbar-mode"
                     Text="{Binding TopBarModeLabel}" />
          <Rectangle Grid.Column="5" Classes="v2-topbar-sep" />
          <StackPanel Grid.Column="6" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
            <Viewbox Width="20" Height="20"><Path Classes="v2-icon v2-icon-muted" Data="{DynamicResource V2.Icon.Clock}" /></Viewbox>
            <TextBlock Classes="v2-body" VerticalAlignment="Center" AutomationProperties.AutomationId="v2-shell-topbar-clock"
                       Text="{Binding RaidClockLabel}" />
          </StackPanel>
          <Rectangle Grid.Column="7" Classes="v2-topbar-sep" />
          <StackPanel Grid.Column="8" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
            <Viewbox Width="20" Height="20"><Path Classes="v2-icon v2-icon-muted" Data="{DynamicResource V2.Icon.Refresh}" /></Viewbox>
            <TextBlock Classes="v2-label" VerticalAlignment="Center" AutomationProperties.AutomationId="v2-shell-topbar-freshness"
                       Text="{Binding DataFreshnessLabel}" />
          </StackPanel>
          <StackPanel Grid.Column="10" Orientation="Horizontal" Spacing="8" IsVisible="{Binding ShowsHeaderSearch}">
            <TextBox Classes="v2-field-input" MinWidth="160" Width="180"
                     Text="{Binding SearchText}"
                     PlaceholderText="{Binding SearchPlaceholder}"
                     AutomationProperties.AutomationId="v2-shell-header-search"
                     AutomationProperties.Name="{Binding SearchLabel}" />
            <Button Classes="v2-target" AutomationProperties.AutomationId="v2-shell-header-search-open"
                    Content="{Binding SearchOpenLabel}" Command="{Binding SearchCommand}" />
          </StackPanel>
          <!-- [#294] Variant B puts Setup in the header rather than the rail, and the header
               draws a label instead of the shared icon template, so the waiting mark is placed
               here too rather than only where the gear is. -->
          <Button Grid.Column="11" Classes="v2-target v2-destination"
                  Classes.selected="{Binding SetupDestination.IsCurrent}"
                  IsVisible="{Binding ShowsHeaderSetup}"
                  AutomationProperties.AutomationId="{Binding SetupDestination.AutomationId}"
                  AutomationProperties.Name="{Binding SetupDestination.Label}"
                  AutomationProperties.ItemStatus="{Binding SetupDestination.StatusDescription}"
                  Command="{Binding SetupDestination.NavigateCommand}">
            <StackPanel Orientation="Horizontal" Spacing="6">
              <TextBlock VerticalAlignment="Center" Text="{Binding SetupDestination.DisplayLabel}" />
              <Ellipse Width="8" Height="8" VerticalAlignment="Center"
                       IsVisible="{Binding SetupDestination.HasNotice}"
                       Fill="{DynamicResource V2.Brush.Info}"
                       AutomationProperties.AccessibilityView="Raw" />
            </StackPanel>
          </Button>
          <!-- #292 task 4: the real chrome's one entry to the command palette (Ctrl+K also
               opens it). Small and tidy on purpose - the owner rejected the old scaffold strip's
               Back/Forward/address/Pin/Copy/Commands row as "100x worse" than V1, and that stays
               reachable only under the developer mode flag (see ShowsScaffoldChrome). This is the
               one piece of it the real chrome keeps, as a single icon button rather than a row. -->
          <Button Grid.Column="12" Classes="v2-target v2-icon-btn v2-topbar-plain" VerticalAlignment="Center"
                  AutomationProperties.AutomationId="v2-shell-topbar-palette" ToolTip.Tip="{Binding PaletteTooltip}"
                  AutomationProperties.Name="{Binding PaletteTooltip}" Command="{Binding PaletteCommand}">
            <Path Classes="v2-icon v2-icon-muted" Data="{DynamicResource V2.Icon.Search}" />
          </Button>
          <Button Grid.Column="13" Classes="v2-target v2-icon-btn v2-topbar-plain" VerticalAlignment="Center"
                  AutomationProperties.AutomationId="v2-shell-capture" ToolTip.Tip="{Binding CaptureLabel}"
                  AutomationProperties.Name="{Binding CaptureLabel}" Command="{Binding CaptureCommand}">
            <Path Classes="v2-icon v2-icon-muted" Data="{DynamicResource V2.Icon.Camera}" />
          </Button>
          <Button Grid.Column="14" Classes="v2-target v2-topbar-plain" VerticalAlignment="Center"
                  AutomationProperties.AutomationId="v2-shell-health"
                  AutomationProperties.Name="{Binding HealthLabel}" Command="{Binding HealthCommand}">
            <Border Classes="v2-status-pill" Classes.v2-availability-ready="{Binding SurfaceIsReady}">
              <StackPanel Orientation="Horizontal" Spacing="8">
                <Ellipse Width="10" Height="10" VerticalAlignment="Center" IsVisible="{Binding SurfaceIsReady}"
                         Fill="{DynamicResource V2.Brush.Success}" AutomationProperties.AccessibilityView="Raw" />
                <TextBlock Classes="v2-glyph" Text="{Binding SurfaceGlyph}" IsVisible="{Binding !SurfaceIsReady}"
                           AutomationProperties.AccessibilityView="Raw" />
                <TextBlock Classes="v2-body" Text="{Binding SurfaceTitle}" />
              </StackPanel>
            </Border>
          </Button>
        </Grid>
      </Border>

      <!-- The #265 A/B scaffold's own chrome: variant/provisional labels, the raw context-field
           strip, Back/Forward, the address bar, Pin/Copy, and the Commands palette. Clayton judged
           this "100x worse" than V1 and nothing like docs/design/v2; it stays reachable only under
           the developer-mode flag for diagnostics and the Windows page gallery script (which now
           passes that flag for the scenarios that still exercise it), never in the normal launch. -->
      <Border Grid.Row="1" Classes="v2-card" CornerRadius="0" IsVisible="{Binding ShowsScaffoldChrome}"
              Padding="{DynamicResource V2.Space.Md.Inset}">
        <StackPanel Classes="v2-stack">
          <StackPanel Classes="v2-stack">
            <TextBlock Classes="v2-label" Text="{Binding VariantName}" />
            <TextBlock Classes="v2-label" Text="{Binding ProvisionalLabel}" />
          </StackPanel>
          <WrapPanel Classes="v2-toolbar"
                     AutomationProperties.AccessibilityView="Control"
                     AutomationProperties.Name="{Binding ContextRegionName}">
            <TextBlock Classes="v2-label" AutomationProperties.AutomationId="v2-shell-context-profile"
                       Text="{Binding ProfileContextLabel}" />
            <TextBlock Classes="v2-label" AutomationProperties.AutomationId="v2-shell-context-local-time"
                       Text="{Binding LocalTimeLabel}" />
            <TextBlock Classes="v2-label" AutomationProperties.AutomationId="v2-shell-context-raid"
                       Text="{Binding RaidContextLabel}" />
            <TextBlock Classes="v2-label" AutomationProperties.AutomationId="v2-shell-context-plan"
                       Text="{Binding PlanContextLabel}" />
            <TextBlock Classes="v2-label" AutomationProperties.AutomationId="v2-shell-context-team"
                       Text="{Binding TeamContextLabel}" />
            <TextBlock Classes="v2-label" AutomationProperties.AutomationId="v2-shell-context-device"
                       Text="{Binding DeviceContextLabel}" />
            <TextBlock Classes="v2-label" AutomationProperties.AutomationId="v2-shell-context-selection"
                       Text="{Binding SelectionContextLabel}" />
          </WrapPanel>
          <WrapPanel Classes="v2-toolbar"
                     HorizontalAlignment="Stretch"
                     AutomationProperties.AccessibilityView="Control"
                     AutomationProperties.ControlTypeOverride="ToolBar"
                     AutomationProperties.Name="{Binding AppName}">
            <Button Classes="v2-target" AutomationProperties.AutomationId="v2-shell-back"
                    AutomationProperties.Name="{Binding BackLabel}" Content="{Binding BackLabel}" Command="{Binding BackCommand}" />
            <Button Classes="v2-target" AutomationProperties.AutomationId="v2-shell-forward"
                    AutomationProperties.Name="{Binding ForwardLabel}" Content="{Binding ForwardLabel}" Command="{Binding ForwardCommand}" />
            <Button Classes="v2-target" AutomationProperties.AutomationId="v2-shell-palette"
                    AutomationProperties.Name="{Binding PaletteLabel}" Content="{Binding PaletteLabel}" Command="{Binding PaletteCommand}" />
          </WrapPanel>
          <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="8">
            <TextBlock Grid.Column="0" Classes="v2-label" VerticalAlignment="Center" Text="{Binding AddressLabel}" />
            <TextBox Grid.Column="1" Classes="v2-field-input" Text="{Binding CurrentAddress}"
                     AutomationProperties.AutomationId="v2-shell-address" AutomationProperties.Name="{Binding AddressLabel}" />
            <Button Grid.Column="2" Classes="v2-target" AutomationProperties.AutomationId="v2-shell-address-open"
                    Content="{Binding OpenAddressLabel}" Command="{Binding AddressCommand}" />
          </Grid>
          <WrapPanel Classes="v2-toolbar">
            <Button Classes="v2-target" AutomationProperties.AutomationId="v2-shell-address-pin"
                    Content="{Binding PinLabel}" Command="{Binding PinCommand}" />
            <Button Classes="v2-target" AutomationProperties.AutomationId="v2-shell-address-copy"
                    Content="{Binding CopyAddressLabel}" Command="{Binding CopyAddressCommand}" />
          </WrapPanel>
        </StackPanel>
      </Border>

      <StackPanel Grid.Row="2" Spacing="{DynamicResource V2.Space.Sm.Gap}">
        <!-- Variant B declares a workflow row. Variant A moves to this same labelled row only when
             the measured content width can no longer hold its declared rail. -->
        <Border Classes="v2-card" CornerRadius="0" IsVisible="{Binding UsesRowNavigation}"
                AutomationProperties.AccessibilityView="Control"
                AutomationProperties.LandmarkType="Navigation"
                AutomationProperties.Name="{Binding NavigationRegionName}">
          <StackPanel Classes="v2-stack">
            <ItemsControl ItemsSource="{Binding PrimaryDestinations}"
                          AutomationProperties.AutomationId="v2-shell-navigation-row">
              <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate><WrapPanel ItemSpacing="8" LineSpacing="8" /></ItemsPanelTemplate>
              </ItemsControl.ItemsPanel>
              <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="vm:V2ShellDestinationViewModel">
                  <!-- V2 rough package 21: plain Label, not DisplayLabel's "› " current-marker
                       prefix, for the same reason the style above reserves the selected border
                       geometry — this button sits in the same WrapPanel, and a per-item content
                       length that changes with IsCurrent (Home is current on Variant B's own
                       landing page) is exactly the "exact wrap boundary" remeasure hazard that
                       comment already describes. The rail already carries the current state with
                       Classes.selected alone; this row now matches it. -->
                  <Button Classes="v2-target v2-destination" Classes.selected="{Binding IsCurrent}"
                          AutomationProperties.AutomationId="{Binding AutomationId}"
                          AutomationProperties.Name="{Binding Label}"
                          AutomationProperties.ItemStatus="{Binding SelectionDescription}"
                          Content="{Binding Label}" Command="{Binding NavigateCommand}" />
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
            <StackPanel Orientation="Horizontal" Spacing="8" IsVisible="{Binding ShowsSeparatedSetup}">
              <TextBlock Classes="v2-label" VerticalAlignment="Center" Text="{Binding SetupSectionLabel}" />
              <Button Classes="v2-target v2-destination" Classes.selected="{Binding SetupDestination.IsCurrent}"
                      AutomationProperties.AutomationId="{Binding SetupDestination.AutomationId}"
                      AutomationProperties.Name="{Binding SetupDestination.Label}"
                      AutomationProperties.ItemStatus="{Binding SetupDestination.StatusDescription}"
                      Content="{Binding SetupDestination.DisplayLabel}" Command="{Binding SetupDestination.NavigateCommand}" />
            </StackPanel>
          </StackPanel>
        </Border>

        <!-- V2 rough package 20: a global problem (a failed endpoint refresh, an unavailable
             surface) reads as ONE single-line banner here and nowhere else. No dashed outline, no
             repeated remainder sub-line, no second copy inside the workspace: the Raid map lost
             about 400px of height to the duplicate that used to sit in the page body below. The
             rest of the story — the remainder line and the checks — is behind the status pill,
             and the line can be dismissed until the problem itself changes. -->
        <Border Classes="v2-surface-banner" IsVisible="{Binding ShowsSurfaceBanner}"
                Classes.v2-tone-warning="{Binding SurfaceIsWarningTone}"
                Classes.v2-tone-danger="{Binding SurfaceIsDangerTone}"
                AutomationProperties.AccessibilityView="Control"
                AutomationProperties.AutomationId="v2-shell-surface-state"
                AutomationProperties.Name="{Binding SurfaceAutomationName}">
          <Grid ColumnDefinitions="Auto,*,Auto,Auto" ColumnSpacing="10">
            <TextBlock Grid.Column="0" Classes="v2-glyph" VerticalAlignment="Center"
                       AutomationProperties.AccessibilityView="Raw" Text="{Binding SurfaceGlyph}" />
            <TextBlock Grid.Column="1" Classes="v2-body" VerticalAlignment="Center"
                       TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"
                       AutomationProperties.AutomationId="v2-shell-surface-state-text"
                       Text="{Binding SurfaceBannerText}" />
            <ItemsControl Grid.Column="2" ItemsSource="{Binding RecoveryActions}" VerticalAlignment="Center">
              <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate><StackPanel Orientation="Horizontal" Spacing="8" /></ItemsPanelTemplate>
              </ItemsControl.ItemsPanel>
              <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="vm:V2RecoveryActionViewModel">
                  <Button Classes="v2-target v2-banner-action" AutomationProperties.AutomationId="{Binding AutomationId}"
                          AutomationProperties.Name="{Binding Label}" Content="{Binding Label}" Command="{Binding InvokeCommand}" />
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Button Grid.Column="3" Classes="v2-target v2-icon-btn v2-topbar-plain" VerticalAlignment="Center"
                    AutomationProperties.AutomationId="v2-shell-surface-state-dismiss"
                    AutomationProperties.Name="{Binding SurfaceDismissLabel}"
                    ToolTip.Tip="{Binding SurfaceDismissLabel}"
                    Command="{Binding DismissSurfaceBannerCommand}">
              <Path Classes="v2-icon v2-icon-muted" Data="{DynamicResource V2.Icon.Close}" />
            </Button>
          </Grid>
        </Border>

        <!-- #562: a paired tablet asking to drive this desktop, wherever the player is looking. -->
        <prim:ControlRequestPromptView DataContext="{Binding ControlRequestPrompt}" />

        <!-- #453: pages that did not load at startup, named, with Retry. -->
        <prim:V2LoadFaultNotice DataContext="{Binding StartupFaultNotice}" />

        <Border Classes="v2-card" IsVisible="{Binding HasPersistenceFailure}"
                AutomationProperties.AccessibilityView="Control"
                AutomationProperties.AutomationId="v2-shell-persistence-failure"
                AutomationProperties.Name="{Binding PersistenceFailure}">
          <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
            <TextBlock Grid.Column="0" Classes="v2-body" Text="{Binding PersistenceFailure}" />
            <Button Grid.Column="1" Classes="v2-target"
                    AutomationProperties.AutomationId="v2-shell-persistence-retry"
                    Content="{Binding PersistenceRetryLabel}" IsEnabled="{Binding CanRetryPersistence}"
                    Command="{Binding RetryPersistenceCommand}" />
          </Grid>
        </Border>
      </StackPanel>

      <Grid Grid.Row="3" ColumnDefinitions="Auto,*">
        <!-- Only Variant A at standard/expanded width exposes this labelled rail. -->
        <!-- [V2 rough package 46] The rail's width is the player's choice now: labelled, icons
             only, or gone. Nothing here is unreachable when it is gone — the launcher over the
             workspace's corner carries the same destinations, and Ctrl+B brings it back. -->
        <Border Grid.Column="0" Width="{Binding NavigationRailWidth}" Classes="v2-shell-rail" Padding="0"
                IsVisible="{Binding ShowsNavigationRail}"
                AutomationProperties.AccessibilityView="Control"
                AutomationProperties.LandmarkType="Navigation"
                AutomationProperties.Name="{Binding NavigationRegionName}">
          <!-- [#607] "the sidebar toggling button should be at the bottom above settings": the
               destinations start at the top, the toggle sits directly above the Settings gear,
               its icon lined up with the gear's, in the labelled and the icons-only rail alike. -->
          <Grid RowDefinitions="*,Auto,Auto">
            <ScrollViewer Grid.Row="0" HorizontalScrollBarVisibility="Disabled">
              <ItemsControl ItemsSource="{Binding PrimaryDestinations}" Margin="0,8,0,12"
                            AutomationProperties.AutomationId="v2-shell-navigation-rail">
                <ItemsControl.ItemTemplate>
                  <DataTemplate x:DataType="vm:V2ShellDestinationViewModel">
                    <Button HorizontalAlignment="Stretch" HorizontalContentAlignment="Left"
                            Classes="v2-target v2-rail-item" Classes.selected="{Binding IsCurrent}"
                            AutomationProperties.AutomationId="{Binding AutomationId}"
                            AutomationProperties.Name="{Binding Label}"
                            AutomationProperties.ItemStatus="{Binding SelectionDescription}"
                            ToolTip.Tip="{Binding Label}"
                            Command="{Binding NavigateCommand}">
                      <StackPanel Orientation="Horizontal" Spacing="14">
                        <ContentControl Content="{Binding}" ContentTemplate="{StaticResource V2RailIconTemplate}"
                                        VerticalAlignment="Center"
                                        AutomationProperties.AccessibilityView="Raw" />
                        <TextBlock Text="{Binding Label}" VerticalAlignment="Center"
                                   IsVisible="{Binding ShowsLabel}" />
                      </StackPanel>
                    </Button>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
            </ScrollViewer>
            <Button Grid.Row="1" Classes="v2-target v2-rail-collapse" HorizontalAlignment="Left" Margin="16,0,0,0"
                    AutomationProperties.AutomationId="v2-shell-navigation-rail-toggle"
                    AutomationProperties.Name="{Binding NavigationRailToggleName}"
                    AutomationProperties.ItemStatus="{Binding NavigationRailStateLabel}"
                    ToolTip.Tip="{Binding NavigationRailToggleName}"
                    Command="{Binding CycleNavigationRailCommand}">
              <Path Classes="v2-icon" Data="{DynamicResource V2.Icon.Sidebar}" />
            </Button>
            <Button Grid.Row="2" Classes="v2-target v2-rail-gear"
                    Classes.selected="{Binding SetupDestination.IsCurrent}"
                    AutomationProperties.AutomationId="{Binding SetupDestination.AutomationId}"
                    AutomationProperties.Name="{Binding SetupDestination.Label}"
                    AutomationProperties.ItemStatus="{Binding SetupDestination.SelectionDescription}"
                    ToolTip.Tip="{Binding SetupDestination.Label}"
                    Command="{Binding SetupDestination.NavigateCommand}">
              <ContentControl Content="{Binding SetupDestination}" ContentTemplate="{StaticResource V2RailIconTemplate}"
                              AutomationProperties.AccessibilityView="Raw" />
            </Button>
          </Grid>
        </Border>

        <!-- [V2 rough package 46] The way back while the rail is away: an 18px edge where the rail
             was, holding one button. Deliberately the same shape as the Raid panel's own handle on
             the other side, and deliberately not floating over the workspace — a button over the
             page's top-left corner sat on top of the section tabs. Its flyout is the same
             destinations, so hiding the rail never hides a place. -->
        <Border Grid.Column="0" Classes="v2-shell-rail-edge" IsVisible="{Binding ShowsNavigationLauncher}">
        <Button Classes="v2-target v2-nav-launcher" VerticalAlignment="Top" Margin="0,8,0,0"
                AutomationProperties.AutomationId="v2-shell-navigation-launcher"
                AutomationProperties.Name="{Binding NavigationLauncherLabel}"
                ToolTip.Tip="{Binding NavigationLauncherLabel}">
          <Button.Flyout>
            <Flyout Placement="BottomEdgeAlignedLeft">
              <StackPanel Spacing="4" MinWidth="180">
                <ItemsControl ItemsSource="{Binding PrimaryDestinations}">
                  <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="vm:V2ShellDestinationViewModel">
                      <Button HorizontalAlignment="Stretch" HorizontalContentAlignment="Left"
                              Classes="v2-target v2-destination" Classes.selected="{Binding IsCurrent}"
                              AutomationProperties.Name="{Binding Label}"
                              AutomationProperties.ItemStatus="{Binding SelectionDescription}"
                              Content="{Binding Label}" Command="{Binding NavigateCommand}" />
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Button HorizontalAlignment="Stretch" HorizontalContentAlignment="Left"
                        Classes="v2-target v2-destination"
                        AutomationProperties.Name="{Binding SetupDestination.Label}"
                        Content="{Binding SetupDestination.Label}" Command="{Binding SetupDestination.NavigateCommand}" />
                <Button HorizontalAlignment="Stretch" HorizontalContentAlignment="Left"
                        Classes="v2-target v2-destination"
                        AutomationProperties.AutomationId="v2-shell-navigation-show-rail"
                        AutomationProperties.Name="{Binding ShowNavigationRailLabel}"
                        Content="{Binding ShowNavigationRailLabel}" Command="{Binding ShowNavigationRailCommand}" />
              </StackPanel>
            </Flyout>
          </Button.Flyout>
          <Path Classes="v2-icon" Data="{DynamicResource V2.Icon.Sidebar}" />
        </Button>
        </Border>

        <Grid Grid.Column="1" ColumnDefinitions="*,Auto"
              AutomationProperties.AccessibilityView="Control"
              AutomationProperties.LandmarkType="Main"
              AutomationProperties.Name="{Binding MainRegionName}">
          <!-- Legacy pages already own their scrolling and need a finite viewport, especially
               the Raid map. Keep the small V2 heading/state strip in the auto row and place the
               legacy page in the remaining row instead of nesting it in another ScrollViewer. -->
          <Grid Grid.Column="0" IsVisible="{Binding ShowsPrimaryContent}" RowDefinitions="Auto,*">
          <ScrollViewer Grid.Row="0" Grid.RowSpan="{Binding ShellBodyRowSpan}"
                        HorizontalScrollBarVisibility="Disabled">
            <StackPanel Classes="v2-page" Margin="16,6,16,0" Spacing="8">
              <!-- The rail already names the current destination, and the section tabs (when a
                   route has any) repeat it as their own first tab — a visible heading here said
                   the same word a third time. It stays in the tree, off-screen, only for the
                   accessibility contract (every page focuses its H1); a screen reader still gets
                   its name, and it draws no focus box, because nothing about landing here was a
                   sighted user's own action. -->
              <TextBlock x:Name="PageHeading" Classes="v2-label"
                         Width="1" Height="1" Opacity="0" IsHitTestVisible="False"
                         Focusable="True" FocusAdorner="{x:Null}"
                         AutomationProperties.AutomationId="v2-shell-page-heading"
                         AutomationProperties.HeadingLevel="1" Text="{Binding CurrentHeading}" />

              <Border Classes="v2-tabbar" IsVisible="{Binding ShowsSectionNavigation}"
                      AutomationProperties.AccessibilityView="Control"
                      AutomationProperties.AutomationId="v2-shell-sections"
                      AutomationProperties.LandmarkType="Navigation"
                      AutomationProperties.Name="{Binding SectionRegionName}">
                <ItemsControl ItemsSource="{Binding SectionItems}">
                  <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate><WrapPanel ItemSpacing="8" LineSpacing="8" /></ItemsPanelTemplate>
                  </ItemsControl.ItemsPanel>
                  <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="vm:V2ShellSectionViewModel">
                      <Button Classes="v2-target v2-tab" Classes.selected="{Binding IsCurrent}"
                              AutomationProperties.AutomationId="{Binding AutomationId}"
                              AutomationProperties.Name="{Binding Label}"
                              AutomationProperties.ItemStatus="{Binding SelectionDescription}"
                              Content="{Binding Label}" Command="{Binding NavigateCommand}" />
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>
              </Border>

              <Border Classes="v2-card" IsVisible="{Binding ShowsReadinessTarget}">
                <StackPanel Classes="v2-stack">
                  <TextBlock Classes="v2-heading-3" Focusable="True"
                             AutomationProperties.AutomationId="{Binding ReadinessTargetAutomationId}"
                             AutomationProperties.Name="{Binding ReadinessTargetAutomationName}"
                             AutomationProperties.HeadingLevel="2"
                             Text="{Binding ReadinessTargetHeading}" />
                  <TextBlock Classes="v2-body" Text="{Binding ReadinessTargetDetail}" />
                </StackPanel>
              </Border>

              <StackPanel Classes="v2-stack" IsVisible="{Binding ShowsReadinessChecklist}">
                <TextBlock Classes="v2-heading-3" AutomationProperties.HeadingLevel="2" Text="{Binding ReadinessHeading}" />
                <TextBlock Classes="v2-body" Text="{Binding ReadinessSummary}"
                           AutomationProperties.AutomationId="v2-shell-readiness-summary"
                           AutomationProperties.Name="{Binding ReadinessSummary}" />
                <ItemsControl ItemsSource="{Binding ReadinessItems}">
                  <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="vm:V2ReadinessCheckViewModel">
                      <Border Classes="v2-card" Margin="0,0,0,8">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="8">
                          <TextBlock Grid.Column="0" Classes="v2-glyph" Text="{Binding Glyph}" AutomationProperties.AccessibilityView="Raw" />
                          <StackPanel Grid.Column="1" Classes="v2-stack">
                            <TextBlock Classes="v2-heading-3" Text="{Binding Label}" />
                            <TextBlock Classes="v2-body" Text="{Binding Detail}" />
                            <TextBlock Classes="v2-label" Text="{Binding Status}" />
                          </StackPanel>
                          <Button Grid.Column="2" Classes="v2-target"
                                  AutomationProperties.AutomationId="{Binding AutomationId}"
                                  AutomationProperties.Name="{Binding AutomationName}"
                                  Content="{Binding ActionLabel}" Command="{Binding OpenCommand}" />
                        </Grid>
                      </Border>
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>
              </StackPanel>

              <StackPanel Classes="v2-stack" IsVisible="{Binding ShowsContinue}">
                <TextBlock Classes="v2-heading-3" AutomationProperties.HeadingLevel="2" Text="{Binding ContinueHeading}" />
                <TextBlock Classes="v2-body" IsVisible="{Binding HasNoContinueItems}" Text="{Binding ContinueEmpty}" />
                <ItemsControl ItemsSource="{Binding ContinueItems}" IsVisible="{Binding HasContinueItems}">
                  <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="vm:V2SavedAddressViewModel">
                      <Button Classes="v2-target" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left"
                              AutomationProperties.AutomationId="{Binding AutomationId}"
                              AutomationProperties.Name="{Binding Address}"
                              Content="{Binding Address}" Command="{Binding OpenCommand}" />
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>
              </StackPanel>

              <!-- V2 rough package 20: the second copy of the global-problem block used to be
                   here. One banner above the workspace is the whole presentation now. -->
            </StackPanel>
          </ScrollViewer>
          <!-- [#294] The host that drew a V1 page inside the V2 shell is gone with the last route
               that named one. The page inset it carried lives on inside each workspace's own
               markup (package 30's Margin="18,6,18,12"), which is what the host contract test
               checks; without it these pages drew flush to the window edge and the right-hand
               primary button was cut off by the frame at 1920 and 3840 alike. -->
          <ContentControl Grid.Row="1" IsVisible="{Binding ShowsWorkspace}" Content="{Binding WorkspaceContent}" />
          <!-- V2 rough package 17: the Intel workspace (search, results, item, prices) binds this
               shell directly; its search, selection and item facts are shell state. -->
          <intelViews:IntelWorkspaceView Grid.Row="1" IsVisible="{Binding ShowsIntelWorkspace}" />
          <!-- V2 Raid cockpit (package 2): a full-page workspace on the same row as the
               workspace host above, mutually exclusive with it by route content. -->
          <ContentControl Grid.Row="1" IsVisible="{Binding ShowsRaidCockpit}" Content="{Binding RaidCockpit}" />
          <ContentControl Grid.Row="1" IsVisible="{Binding ShowsLootScan}" Content="{Binding LootScanResult}" />
          <!-- [#572] "Scanning · Matching icons" while a loot scan runs, over whatever the page shows. -->
          <Panel Grid.Row="1" IsVisible="{Binding ShowsLootScan}" HorizontalAlignment="Right" VerticalAlignment="Top">
            <Border Classes="v2-card" Margin="0,10,28,0" Padding="12,6"
                    IsVisible="{Binding LootScanProgress.IsVisible, FallbackValue=False}"
                    AutomationProperties.AutomationId="v2-loot-scan-progress" AutomationProperties.LiveSetting="Polite">
              <TextBlock Classes="v2-label" Text="{Binding LootScanProgress.Text, FallbackValue=''}" />
            </Border>
          </Panel>
          <!-- V2 rough package 17 (scan): a quiet empty state with the one action that fills it. -->
          <StackPanel Grid.Row="1" IsVisible="{Binding ShowsLootScanEmpty}" Spacing="10" MaxWidth="420"
                      HorizontalAlignment="Center" VerticalAlignment="Center">
            <Viewbox Width="44" Height="44" HorizontalAlignment="Center">
              <Path Classes="v2-icon v2-icon-muted" Data="{DynamicResource V2.Icon.Camera}" />
            </Viewbox>
            <TextBlock Classes="v2-heading-3" HorizontalAlignment="Center" Text="{Binding LootScanEmptyTitle}" />
            <TextBlock Classes="v2-label" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"
                       Text="{Binding LootScanEmptyLabel}" />
            <Button Classes="v2-outline" HorizontalAlignment="Center" Margin="0,6,0,0"
                    AutomationProperties.AutomationId="v2-loot-scan-start"
                    Content="{Binding LootScanActionLabel}" Command="{Binding ScanLootCommand}" />
          </StackPanel>
          <ScrollViewer Grid.Row="1" IsVisible="{Binding ShowsSetupWorkspace}" HorizontalScrollBarVisibility="Disabled">
            <ContentControl Content="{Binding SetupWorkspace}" />
          </ScrollViewer>
          </Grid>

          <!-- The Intel card for an item opened beside some other page changes columns. At wide B it
               is beside the current page; at a narrow width it spans the content and replaces it.
               On the Intel routes themselves the workspace above shows the item instead. -->
          <ScrollViewer Grid.Column="{Binding IntelColumn}" Grid.ColumnSpan="{Binding IntelColumnSpan}"
                        IsVisible="{Binding ShowsIntelCard}" HorizontalScrollBarVisibility="Disabled">
            <Border Classes="v2-card" Margin="12" MaxWidth="520" HorizontalAlignment="Stretch">
              <StackPanel Classes="v2-stack">
                <TextBlock x:Name="IntelHeading" Classes="v2-heading-2" Focusable="True"
                           AutomationProperties.AutomationId="v2-shell-intel-heading"
                           AutomationProperties.HeadingLevel="1" Text="{Binding IntelHeading}" />
                <TextBlock Classes="v2-body" Text="{Binding IntelDescription}" />
                <TextBlock Classes="v2-label" IsVisible="{Binding IntelStatusLabel, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                           Text="{Binding IntelStatusLabel}" />
                <TextBlock Classes="v2-label" IsVisible="{Binding IntelKindLabel, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                           Text="{Binding IntelKindLabel}" />
                <ItemsControl ItemsSource="{Binding IntelFacts}"
                              AutomationProperties.AutomationId="v2-shell-intel-facts">
                  <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="vm:V2ShellIntelFactViewModel">
                      <Grid ColumnDefinitions="Auto,*">
                        <TextBlock Grid.Column="0" Classes="v2-label" Text="{Binding Label}" Margin="0,0,8,0" />
                        <TextBlock Grid.Column="1" Classes="v2-body" Text="{Binding Value}" TextWrapping="Wrap" />
                      </Grid>
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Button Classes="v2-target" HorizontalAlignment="Left"
                        IsVisible="{Binding IntelHasWikiLink}"
                        AutomationProperties.AutomationId="v2-shell-intel-wiki"
                        Content="{Binding IntelWikiLabel}" Command="{Binding OpenIntelWikiCommand}" />
                <Button Classes="v2-target" HorizontalAlignment="Left"
                        AutomationProperties.AutomationId="v2-shell-intel-close"
                        Content="{Binding CloseLabel}" Command="{Binding CloseTransientCommand}" />
              </StackPanel>
            </Border>
          </ScrollViewer>
        </Grid>
      </Grid>
    </Grid>

    <!-- Exactly one modal layer. Its three mutually exclusive bodies have distinct title ids so
         focus requests and packaged UI Automation can prove which dialog actually opened. -->
    <Border Classes="v2-shell-dialog-scrim" IsVisible="{Binding HasOpenDialog}" ZIndex="10">
      <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
        <Border Classes="v2-card" HorizontalAlignment="Stretch" VerticalAlignment="Center" MaxWidth="720"
                AutomationProperties.AccessibilityView="Control"
                AutomationProperties.AutomationId="v2-shell-dialog"
                AutomationProperties.ControlTypeOverride="Window"
                AutomationProperties.Name="{Binding DialogAutomationName}">
          <Grid>
          <StackPanel Classes="v2-stack" IsVisible="{Binding IsCaptureOpen}" AutomationProperties.Name="{Binding CaptureHeading}">
            <TextBlock Classes="v2-heading-2" Focusable="True"
                       AutomationProperties.AutomationId="v2-shell-capture-dialog-title"
                       AutomationProperties.HeadingLevel="1" Text="{Binding CaptureHeading}" />
            <TextBlock Classes="v2-body" Text="{Binding CaptureGuidance}" />
            <TextBlock Classes="v2-label" Text="{Binding CaptureWatchingStatus}" />
            <TextBlock Classes="v2-label" Text="{Binding CaptureArmedStatus}" />
            <TextBlock Classes="v2-label" Text="{Binding CaptureShortcutStatus}" />
            <TextBlock Classes="v2-heading-3" Text="{Binding CaptureIntentHeading}" />
            <ItemsControl ItemsSource="{Binding CaptureIntents}">
              <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate><WrapPanel ItemSpacing="8" LineSpacing="8" /></ItemsPanelTemplate>
              </ItemsControl.ItemsPanel>
              <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="vm:V2CaptureIntentViewModel">
                  <RadioButton Classes="v2-target" GroupName="v2-capture-intent"
                               AutomationProperties.AutomationId="{Binding AutomationId}"
                               AutomationProperties.Name="{Binding Label}"
                               Content="{Binding Label}" IsChecked="{Binding IsSelected, Mode=TwoWay}"
                               IsEnabled="{Binding IsSupported}" />
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Button Classes="v2-target" HorizontalAlignment="Left"
                    AutomationProperties.AutomationId="v2-shell-capture-arm"
                    Content="{Binding CaptureArmLabel}" Command="{Binding ArmCaptureCommand}" />
            <!-- [f920 capture] A picture the player already has: paste, drop or choose. -->
            <Border Classes="v2-card" AutomationProperties.AutomationId="v2-shell-capture-manual">
              <StackPanel Classes="v2-stack">
                <TextBlock Classes="v2-heading-3" Text="{Binding CaptureManualHeading}" />
                <TextBlock Classes="v2-label" Text="{Binding CaptureManualHint}" TextWrapping="Wrap" />
                <Button Classes="v2-target" HorizontalAlignment="Left"
                        AutomationProperties.AutomationId="v2-shell-capture-pick"
                        Content="{Binding CapturePickLabel}" Command="{Binding PickCaptureImageCommand}" />
                <TextBlock Classes="v2-body" IsVisible="{Binding HasCaptureManualStatus}"
                           AutomationProperties.AutomationId="v2-shell-capture-manual-status"
                           Text="{Binding CaptureManualStatus}" TextWrapping="Wrap" />
              </StackPanel>
            </Border>
            <Border Classes="v2-card" IsVisible="{Binding HasCaptureAttention}"
                    AutomationProperties.AccessibilityView="Control"
                    AutomationProperties.AutomationId="v2-shell-capture-attention"
                    AutomationProperties.Name="{Binding CaptureAttentionHeading}">
              <StackPanel Classes="v2-stack">
                <TextBlock Classes="v2-heading-3" Text="{Binding CaptureAttentionHeading}" />
                <TextBlock Classes="v2-body" Text="{Binding CaptureAttentionDetail}" />
                <ItemsControl ItemsSource="{Binding CaptureAttentionActions}">
                  <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate><WrapPanel ItemSpacing="8" LineSpacing="8" /></ItemsPanelTemplate>
                  </ItemsControl.ItemsPanel>
                  <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="vm:V2CaptureActionViewModel">
                      <Button Classes="v2-target" AutomationProperties.AutomationId="{Binding AutomationId}"
                              AutomationProperties.Name="{Binding Label}" Content="{Binding Label}"
                              Command="{Binding InvokeCommand}" />
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>
              </StackPanel>
            </Border>
            <TextBlock Classes="v2-heading-3" IsVisible="{Binding HasCaptureProgress}"
                       Text="{Binding CaptureProgressHeading}" />
            <ItemsControl ItemsSource="{Binding CaptureProgressItems}" IsVisible="{Binding HasCaptureProgress}">
              <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="vm:V2CaptureProgressViewModel">
                  <Border Classes="v2-card" AutomationProperties.AutomationId="{Binding AutomationId}">
                    <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="8">
                      <TextBlock Grid.Column="0" Classes="v2-label" Text="{Binding Stage}" />
                      <StackPanel Grid.Column="1" Classes="v2-stack">
                        <TextBlock Classes="v2-body" Text="{Binding Detail}" />
                        <TextBlock Classes="v2-label" Text="{Binding Artifact}" />
                      </StackPanel>
                      <TextBlock Grid.Column="2" Classes="v2-label" Text="{Binding Percent}" />
                    </Grid>
                  </Border>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Border Classes="v2-card" IsVisible="{Binding HasCaptureReview}"
                    AutomationProperties.AccessibilityView="Control"
                    AutomationProperties.AutomationId="v2-shell-capture-review">
              <StackPanel Classes="v2-stack">
                <TextBlock Classes="v2-heading-3" Text="{Binding CaptureReviewHeading}" />
                <TextBlock Classes="v2-body" Text="{Binding CaptureReviewSummary}" />
                <TextBlock Classes="v2-label" Text="{Binding CaptureReviewEvidence}" />
                <TextBlock Classes="v2-label" IsVisible="{Binding HasCaptureReviewCandidates}"
                           Text="{Binding CaptureCandidatesHeading}" />
                <ItemsControl ItemsSource="{Binding CaptureReviewCandidates}" IsVisible="{Binding HasCaptureReviewCandidates}">
                  <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate><WrapPanel ItemSpacing="8" LineSpacing="8" /></ItemsPanelTemplate>
                  </ItemsControl.ItemsPanel>
                  <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="vm:V2CaptureCandidateViewModel">
                      <RadioButton Classes="v2-target" GroupName="v2-capture-candidate"
                                   AutomationProperties.AutomationId="{Binding AutomationId}"
                                   AutomationProperties.Name="{Binding Label}" Content="{Binding Label}"
                                   IsChecked="{Binding IsChosen, Mode=OneWay}" Command="{Binding ChooseCommand}" />
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>
                <ItemsControl ItemsSource="{Binding CaptureReviewActions}">
                  <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate><WrapPanel ItemSpacing="8" LineSpacing="8" /></ItemsPanelTemplate>
                  </ItemsControl.ItemsPanel>
                  <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="vm:V2CaptureActionViewModel">
                      <Button Classes="v2-target" AutomationProperties.AutomationId="{Binding AutomationId}"
                              AutomationProperties.Name="{Binding Label}" Content="{Binding Label}"
                              Command="{Binding InvokeCommand}" />
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>
              </StackPanel>
            </Border>
            <TextBlock Classes="v2-heading-3" Text="{Binding CapturePriorHeading}" />
            <TextBlock Classes="v2-body" Text="{Binding CapturePrior}" />
            <TextBlock Classes="v2-label" IsVisible="{Binding HasCaptureReference}" Text="{Binding CaptureReference}" />
            <Button Classes="v2-target" HorizontalAlignment="Left"
                    AutomationProperties.AutomationId="v2-shell-capture-dialog-close"
                    Content="{Binding CloseLabel}" Command="{Binding CloseTransientCommand}" />
          </StackPanel>

          <StackPanel Classes="v2-stack" IsVisible="{Binding IsPaletteOpen}" AutomationProperties.Name="{Binding PaletteHeading}">
            <TextBlock Classes="v2-heading-2" Focusable="True"
                       AutomationProperties.AutomationId="v2-shell-palette-dialog-title"
                       AutomationProperties.HeadingLevel="1" Text="{Binding PaletteHeading}" />
            <TextBox Classes="v2-field-input" Text="{Binding PaletteQuery}"
                     PlaceholderText="{Binding PaletteQueryLabel}"
                     AutomationProperties.AutomationId="v2-shell-palette-query"
                     AutomationProperties.Name="{Binding PaletteQueryLabel}" />
            <Button Classes="v2-target" HorizontalAlignment="Left"
                    AutomationProperties.AutomationId="v2-shell-palette-open-address"
                    Content="{Binding OpenAddressLabel}" Command="{Binding PaletteAddressCommand}" />
            <TextBlock Classes="v2-label" IsVisible="{Binding HasNoFilteredCommandItems}" Text="{Binding PaletteEmpty}" />
            <ScrollViewer MaxHeight="240" HorizontalScrollBarVisibility="Disabled" IsVisible="{Binding HasFilteredCommandItems}">
              <ItemsControl ItemsSource="{Binding FilteredCommandItems}">
                <ItemsControl.ItemTemplate>
                  <DataTemplate x:DataType="vm:V2ShellCommandViewModel">
                    <Button Classes="v2-target" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch"
                            AutomationProperties.AutomationId="{Binding AutomationId}"
                            AutomationProperties.Name="{Binding Label}" Command="{Binding InvokeCommand}">
                      <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
                        <TextBlock Grid.Column="0" Classes="v2-body" Text="{Binding Label}" />
                        <TextBlock Grid.Column="1" Classes="v2-label" Text="{Binding Gesture}" />
                      </Grid>
                    </Button>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
            </ScrollViewer>
            <TextBlock Classes="v2-heading-3" IsVisible="{Binding HasPins}" Text="{Binding PalettePinsHeading}" />
            <ItemsControl ItemsSource="{Binding PinItems}" IsVisible="{Binding HasPins}">
              <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="vm:V2SavedAddressViewModel">
                  <Button Classes="v2-target" AutomationProperties.AutomationId="{Binding PaletteAutomationId}"
                          AutomationProperties.Name="{Binding Address}" Content="{Binding Address}" Command="{Binding OpenCommand}" />
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
            <TextBlock Classes="v2-heading-3" IsVisible="{Binding HasRecents}" Text="{Binding PaletteRecentsHeading}" />
            <ItemsControl ItemsSource="{Binding RecentItems}" IsVisible="{Binding HasRecents}">
              <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="vm:V2SavedAddressViewModel">
                  <Button Classes="v2-target" AutomationProperties.AutomationId="{Binding PaletteAutomationId}"
                          AutomationProperties.Name="{Binding Address}" Content="{Binding Address}" Command="{Binding OpenCommand}" />
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Button Classes="v2-target" HorizontalAlignment="Left"
                    AutomationProperties.AutomationId="v2-shell-palette-dialog-close"
                    Content="{Binding CloseLabel}" Command="{Binding CloseTransientCommand}" />
          </StackPanel>

          <StackPanel Classes="v2-stack" IsVisible="{Binding IsHealthOpen}" AutomationProperties.Name="{Binding HealthHeading}">
            <TextBlock Classes="v2-heading-2" Focusable="True"
                       AutomationProperties.AutomationId="v2-shell-health-dialog-title"
                       AutomationProperties.HeadingLevel="1" Text="{Binding HealthHeading}" />
            <TextBlock Classes="v2-body" Text="{Binding HealthSummary}" />
            <!-- V2 rough package 20: the banner is one line, so the current problem's full detail,
                 what still works, and its recovery actions live here, behind the status pill. -->
            <Border Classes="v2-card" IsVisible="{Binding ShowsHealthSurface}"
                    AutomationProperties.AccessibilityView="Control"
                    AutomationProperties.AutomationId="v2-shell-health-surface"
                    AutomationProperties.Name="{Binding SurfaceAutomationName}">
              <StackPanel Classes="v2-stack">
                <StackPanel Orientation="Horizontal" Spacing="8">
                  <TextBlock Classes="v2-glyph" AutomationProperties.AccessibilityView="Raw" Text="{Binding SurfaceGlyph}" />
                  <TextBlock Classes="v2-heading-3" Text="{Binding SurfaceTitle}" />
                </StackPanel>
                <TextBlock Classes="v2-body" TextWrapping="Wrap" Text="{Binding Surface.Detail}" />
                <TextBlock Classes="v2-label" TextWrapping="Wrap" Text="{Binding SurfaceRemainder}" />
                <ItemsControl ItemsSource="{Binding RecoveryActions}">
                  <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate><WrapPanel ItemSpacing="8" LineSpacing="8" /></ItemsPanelTemplate>
                  </ItemsControl.ItemsPanel>
                  <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="vm:V2RecoveryActionViewModel">
                      <!-- No AutomationId: the banner's own buttons already carry these ids, and a
                           duplicate would make either one ambiguous to UI Automation. -->
                      <Button Classes="v2-target" AutomationProperties.Name="{Binding Label}"
                              Content="{Binding Label}" Command="{Binding InvokeCommand}" />
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>
              </StackPanel>
            </Border>
            <ItemsControl ItemsSource="{Binding ReadinessItems}">
              <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="vm:V2ReadinessCheckViewModel">
                  <Button Classes="v2-target" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch"
                          AutomationProperties.AutomationId="{Binding DialogAutomationId}"
                          AutomationProperties.Name="{Binding AutomationName}" Command="{Binding OpenCommand}">
                    <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="8">
                      <TextBlock Grid.Column="0" Classes="v2-glyph" Text="{Binding Glyph}" AutomationProperties.AccessibilityView="Raw" />
                      <TextBlock Grid.Column="1" Classes="v2-body" Text="{Binding Label}" />
                      <TextBlock Grid.Column="2" Classes="v2-label" Text="{Binding Status}" />
                    </Grid>
                  </Button>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Button Classes="v2-target" HorizontalAlignment="Left"
                    AutomationProperties.AutomationId="v2-shell-health-dialog-close"
                    Content="{Binding CloseLabel}" Command="{Binding CloseTransientCommand}" />
          </StackPanel>
          </Grid>
        </Border>
      </ScrollViewer>
    </Border>

    <TextBlock Classes="v2-label"
               Width="1" Height="1" Opacity="0" IsHitTestVisible="False"
               HorizontalAlignment="Left" VerticalAlignment="Bottom"
               AutomationProperties.AutomationId="v2-shell-live-polite"
               AutomationProperties.LiveSetting="Polite"
               Text="{Binding PoliteAnnouncement}" />
    <TextBlock Classes="v2-label"
               Width="1" Height="1" Opacity="0" IsHitTestVisible="False"
               HorizontalAlignment="Left" VerticalAlignment="Bottom"
               AutomationProperties.AutomationId="v2-shell-live-assertive"
               AutomationProperties.LiveSetting="Assertive"
               Text="{Binding AssertiveAnnouncement}" />
  </Grid>
</UserControl>
