<UserControl x:Class="TarkovCompanion.App.Views.V2.Plan.PlanWorkspaceView"
             x:DataType="vm:PlanWorkspaceViewModel"
             xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:TarkovCompanion.App.ViewModels.V2.Plan">
  <UserControl.Styles>
    <!-- A bundle card in the left column: flat surface, cyan outline only while selected. -->
    <Style Selector="Button.v2-plan-bundle">
      <Setter Property="Background" Value="{DynamicResource V2.Brush.Surface}" />
      <Setter Property="BorderBrush" Value="{DynamicResource V2.Brush.SurfaceRaised}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="CornerRadius" Value="{DynamicResource V2.Shape.Card}" />
      <Setter Property="Padding" Value="10" />
      <Setter Property="HorizontalAlignment" Value="Stretch" />
      <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    </Style>
    <Style Selector="Button.v2-plan-bundle.selected">
      <Setter Property="BorderBrush" Value="{DynamicResource V2.Brush.Action}" />
      <Setter Property="BorderThickness" Value="2" />
      <Setter Property="Padding" Value="9" />
    </Style>
    <Style Selector="Border.v2-plan-tile">
      <Setter Property="Width" Value="48" />
      <Setter Property="Height" Value="48" />
      <Setter Property="Background" Value="{DynamicResource V2.Brush.SurfaceRaised}" />
      <Setter Property="CornerRadius" Value="{DynamicResource V2.Shape.Control}" />
    </Style>
    <!-- Flat text actions on an objective row; the row, not a wall of boxes, is the unit. -->
    <Style Selector="Button.v2-plan-link">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Padding" Value="8,4" />
      <Setter Property="Foreground" Value="{DynamicResource V2.Brush.TextSecondary}" />
    </Style>
    <Style Selector="Button.v2-plan-link.v2-plan-link-action">
      <Setter Property="Foreground" Value="{DynamicResource V2.Brush.Action}" />
    </Style>
    <Style Selector="Border.v2-plan-card">
      <Setter Property="Background" Value="{DynamicResource V2.Brush.Surface}" />
      <Setter Property="BorderBrush" Value="{DynamicResource V2.Brush.SurfaceRaised}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="CornerRadius" Value="{DynamicResource V2.Shape.Card}" />
    </Style>
    <Style Selector="Border.v2-plan-section">
      <Setter Property="Background" Value="{DynamicResource V2.Brush.Surface}" />
      <Setter Property="BorderBrush" Value="{DynamicResource V2.Brush.SurfaceRaised}" />
      <Setter Property="BorderThickness" Value="3,1,1,1" />
      <Setter Property="CornerRadius" Value="{DynamicResource V2.Shape.Card}" />
      <Setter Property="Padding" Value="16,12" />
    </Style>
    <Style Selector="Border.v2-plan-section.v2-plan-accent">
      <Setter Property="BorderBrush" Value="{DynamicResource V2.Brush.Action}" />
    </Style>
    <Style Selector="Border.v2-plan-panel">
      <Setter Property="Background" Value="{DynamicResource V2.Brush.Surface}" />
      <Setter Property="BorderBrush" Value="{DynamicResource V2.Brush.SurfaceRaised}" />
      <Setter Property="BorderThickness" Value="1,0,0,0" />
    </Style>
    <Style Selector="Border.v2-plan-step-row">
      <Setter Property="Padding" Value="12,10" />
      <Setter Property="CornerRadius" Value="{DynamicResource V2.Shape.Control}" />
      <Setter Property="Background" Value="Transparent" />
    </Style>
    <Style Selector="Border.v2-plan-step-row:pointerover">
      <Setter Property="Background" Value="{DynamicResource V2.Brush.SurfaceRaised}" />
    </Style>
    <Style Selector="Border.v2-plan-chip">
      <Setter Property="BorderBrush" Value="{DynamicResource V2.Brush.Border}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="CornerRadius" Value="{DynamicResource V2.Shape.Control}" />
      <Setter Property="Padding" Value="8,1" />
    </Style>
    <!-- The primary action, the one filled cyan control on the page, like the concept's "Open in Raid". -->
    <Style Selector="Button.v2-plan-primary">
      <Setter Property="Background" Value="{DynamicResource V2.Brush.Action}" />
      <Setter Property="Foreground" Value="{DynamicResource V2.Brush.Canvas}" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="CornerRadius" Value="{DynamicResource V2.Shape.Control}" />
      <Setter Property="Height" Value="52" />
      <Setter Property="HorizontalAlignment" Value="Stretch" />
      <Setter Property="HorizontalContentAlignment" Value="Center" />
      <Setter Property="FontSize" Value="18" />
      <Setter Property="FontWeight" Value="SemiBold" />
    </Style>
    <Style Selector="Button.v2-plan-primary:pointerover /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="Background" Value="{DynamicResource V2.Brush.Focus}" />
      <Setter Property="Foreground" Value="{DynamicResource V2.Brush.Canvas}" />
    </Style>
    <Style Selector="Button.v2-plan-primary Path.v2-icon">
      <Setter Property="Stroke" Value="{DynamicResource V2.Brush.Canvas}" />
      <Setter Property="Fill" Value="{DynamicResource V2.Brush.Canvas}" />
    </Style>
    <Style Selector="ToggleSwitch.v2-plan-switch">
      <Setter Property="MinWidth" Value="0" />
    </Style>
  </UserControl.Styles>

  <!-- V2 rough package 17: laid out like docs/design/v2/v2-plan-workspace-concept.png. Left, the
       "next raid" column of map bundles (one per map with active objectives) plus the Hideout card;
       centre, the selected map's objectives as numbered steps; right, a context panel with that
       map's quests and the one primary action, Open in Raid. The concept's centre map, traffic,
       loadout readiness and route estimate have no data source yet, so they are not drawn. -->
  <Grid ColumnDefinitions="340,*,Auto">
    <!-- Left column -->
    <Border Grid.Column="0" BorderBrush="{DynamicResource V2.Brush.SurfaceRaised}" BorderThickness="0,0,1,0">
      <Grid RowDefinitions="*,Auto">
        <ScrollViewer Grid.Row="0" HorizontalScrollBarVisibility="Disabled">
          <StackPanel Spacing="12" Margin="16,8,16,16">
            <Grid ColumnDefinitions="*,Auto">
              <TextBlock Classes="v2-heading-1" Text="Next raid" AutomationProperties.HeadingLevel="1"
                         VerticalAlignment="Center" />
              <Button Grid.Column="1" Classes="v2-target v2-icon-btn v2-plan-link" VerticalAlignment="Center"
                      AutomationProperties.AutomationId="v2-plan-refresh" AutomationProperties.Name="Refresh"
                      ToolTip.Tip="Refresh" Command="{Binding RefreshCommand}">
                <Path Classes="v2-icon v2-icon-muted" Data="{DynamicResource V2.Icon.Refresh}" />
              </Button>
            </Grid>
            <StackPanel Spacing="2">
              <TextBlock Classes="v2-body" Text="{Binding Status}" TextWrapping="Wrap"
                         AutomationProperties.AutomationId="v2-plan-status"
                         AutomationProperties.LiveSetting="Polite" />
              <TextBlock Classes="v2-label" Text="{Binding ScopeLabel}" />
            </StackPanel>

            <Rectangle Height="1" Fill="{DynamicResource V2.Brush.SurfaceRaised}" Margin="0,4" />
            <TextBlock Classes="v2-heading-3" Text="By map" AutomationProperties.HeadingLevel="2"
                       IsVisible="{Binding HasGroups}" />
            <ItemsControl ItemsSource="{Binding Groups}" IsVisible="{Binding HasGroups}">
              <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate><StackPanel Spacing="10" /></ItemsPanelTemplate>
              </ItemsControl.ItemsPanel>
              <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="vm:PlanMapGroupViewModel">
                  <Button Classes="v2-target v2-plan-bundle" Classes.selected="{Binding IsSelected}"
                          Command="{Binding SelectCommand}"
                          AutomationProperties.Name="{Binding MapLabel}"
                          AutomationProperties.ItemStatus="{Binding Summary}">
                    <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                      <Border Classes="v2-plan-tile">
                        <Path Classes="v2-icon v2-icon-muted" Classes.v2-icon-action="{Binding IsSelected}"
                              Data="{DynamicResource V2.Icon.Map}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                      </Border>
                      <StackPanel Grid.Column="1" Spacing="4" VerticalAlignment="Center">
                        <TextBlock Classes="v2-body" FontWeight="SemiBold" Text="{Binding MapLabel}"
                                   TextTrimming="CharacterEllipsis" />
                        <TextBlock Classes="v2-label" Text="{Binding Summary}" />
                      </StackPanel>
                      <Path Grid.Column="2" Classes="v2-icon v2-icon-muted" Classes.v2-icon-action="{Binding IsSelected}"
                            Data="{DynamicResource V2.Icon.ChevronRight}" VerticalAlignment="Center" />
                    </Grid>
                  </Button>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Button Classes="v2-target v2-plan-bundle" Command="{Binding OpenHideoutCommand}"
                    AutomationProperties.AutomationId="v2-plan-open-hideout"
                    AutomationProperties.Name="Hideout">
              <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                <Border Classes="v2-plan-tile">
                  <Path Classes="v2-icon v2-icon-muted" Data="{DynamicResource V2.Icon.House}"
                        HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
                <StackPanel Grid.Column="1" Spacing="4" VerticalAlignment="Center">
                  <TextBlock Classes="v2-body" FontWeight="SemiBold" Text="Hideout" />
                  <TextBlock Classes="v2-label" Text="Stations and upgrade items" />
                </StackPanel>
                <Path Grid.Column="2" Classes="v2-icon v2-icon-muted" Data="{DynamicResource V2.Icon.ChevronRight}"
                      VerticalAlignment="Center" />
              </Grid>
            </Button>
          </StackPanel>
        </ScrollViewer>
        <Border Grid.Row="1" BorderBrush="{DynamicResource V2.Brush.SurfaceRaised}" BorderThickness="0,1,0,0"
                Padding="16,8">
          <ToggleSwitch Classes="v2-plan-switch" AutomationProperties.AutomationId="v2-plan-show-all"
                        AutomationProperties.Name="Show everything"
                        OnContent="Show everything" OffContent="Show everything"
                        IsChecked="{Binding ShowAll}" />
        </Border>
      </Grid>
    </Border>

    <!-- Centre: the selected map's objectives as numbered steps. -->
    <Border Grid.Column="1" Classes="v2-plan-card" Margin="16,8,16,16">
      <Grid>
        <TextBlock Classes="v2-body" IsVisible="{Binding !HasGroups}" TextWrapping="Wrap" TextAlignment="Center"
                   HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="420" Opacity="0.8"
                   Text="Nothing planned yet. Active quest objectives appear here, grouped by map." />
        <Grid RowDefinitions="Auto,*" IsVisible="{Binding HasSelectedGroup}">
          <Grid Grid.Row="0" ColumnDefinitions="*,Auto" Margin="20,16,20,8" DataContext="{Binding SelectedGroup}">
            <StackPanel x:DataType="vm:PlanMapGroupViewModel" Spacing="2">
              <TextBlock Classes="v2-heading-2" Text="Objectives" AutomationProperties.HeadingLevel="2" />
              <TextBlock Classes="v2-label" Text="{Binding Summary}" />
            </StackPanel>
          </Grid>
          <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Disabled" DataContext="{Binding SelectedGroup}">
            <ItemsControl x:DataType="vm:PlanMapGroupViewModel" ItemsSource="{Binding Objectives}" Margin="8,0,8,12"
                          MaxWidth="1400" HorizontalAlignment="Left">
              <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="vm:PlanObjectiveRowViewModel">
                  <Grid ColumnDefinitions="Auto,*">
                    <!-- The step rail: a numbered ring, joined to the next by a line. -->
                    <Grid Grid.Column="0" RowDefinitions="Auto,*" Width="44" Margin="4,0,0,0">
                      <Panel Grid.Row="0" Width="32" Height="32" Margin="0,10,0,0">
                        <Ellipse Stroke="{DynamicResource V2.Brush.Action}" StrokeThickness="2" />
                        <TextBlock Classes="v2-body" FontWeight="SemiBold" Text="{Binding Number}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center" />
                      </Panel>
                      <Rectangle Grid.Row="1" Width="2" Fill="{DynamicResource V2.Brush.Action}" Opacity="0.45"
                                 HorizontalAlignment="Center" Margin="0,4,0,-10" IsVisible="{Binding HasNext}" />
                    </Grid>
                    <Border Grid.Column="1" Classes="v2-plan-step-row" Margin="0,0,0,4">
                      <Grid ColumnDefinitions="*,Auto" ColumnSpacing="16">
                        <StackPanel Grid.Column="0" Spacing="4">
                          <WrapPanel ItemSpacing="10">
                            <TextBlock Classes="v2-body" FontWeight="SemiBold" Text="{Binding TaskName}" />
                            <TextBlock Classes="v2-label" Text="{Binding TraderLabel}" VerticalAlignment="Center" />
                          </WrapPanel>
                          <TextBlock Classes="v2-body" Text="{Binding Description}" TextWrapping="Wrap"
                                     Foreground="{DynamicResource V2.Brush.TextSecondary}" />
                          <WrapPanel ItemSpacing="8" LineSpacing="4" Margin="0,2,0,0">
                            <Border Classes="v2-plan-chip" IsVisible="{Binding HasHandlingLabel}">
                              <TextBlock Classes="v2-label" Text="{Binding HandlingLabel}" />
                            </Border>
                            <TextBlock Classes="v2-label" Text="{Binding RemainingLabel}" VerticalAlignment="Center"
                                       IsVisible="{Binding HasRemainingLabel}" />
                            <TextBlock Classes="v2-label" Text="Unsupported objective" IsVisible="{Binding IsUnsupported}"
                                       VerticalAlignment="Center" />
                          </WrapPanel>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="2" VerticalAlignment="Top">
                          <Button Classes="v2-target v2-plan-link" Command="{Binding MarkObjectiveDoneCommand}"
                                  AutomationProperties.Name="Mark objective done" ToolTip.Tip="Mark objective done">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                              <Viewbox Width="18" Height="18">
                                <Path Classes="v2-icon v2-icon-muted" Data="{DynamicResource V2.Icon.Check}" />
                              </Viewbox>
                              <TextBlock Text="Done" VerticalAlignment="Center" />
                            </StackPanel>
                          </Button>
                          <Button Classes="v2-target v2-icon-btn v2-plan-link" AutomationProperties.Name="More actions"
                                  ToolTip.Tip="More actions">
                            <Path Classes="v2-icon v2-icon-muted" StrokeThickness="3" Data="{DynamicResource V2.Icon.More}" />
                            <Button.Flyout>
                              <MenuFlyout Placement="BottomEdgeAlignedRight">
                                <MenuItem Header="Mark quest done" Command="{Binding MarkQuestDoneCommand}" />
                                <MenuItem Header="Show on map" Command="{Binding ShowOnMapCommand}"
                                          IsVisible="{Binding CanShowOnMap}" />
                                <MenuItem Header="Open wiki" Command="{Binding OpenWikiCommand}"
                                          IsVisible="{Binding HasWikiLink}" />
                              </MenuFlyout>
                            </Button.Flyout>
                          </Button>
                        </StackPanel>
                      </Grid>
                    </Border>
                  </Grid>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </ScrollViewer>
        </Grid>
      </Grid>
    </Border>

    <!-- Right: the selected map's context panel. -->
    <Border Grid.Column="2" Width="420" Classes="v2-plan-panel">
      <Panel>
        <TextBlock Classes="v2-label" Margin="16" IsVisible="{Binding !HasSelectedGroup}" TextWrapping="Wrap"
                   Text="Pick a map to see its quests." />
        <!-- IsVisible on this wrapper, DataContext on the grid inside: one element carrying both
             would resolve IsVisible against SelectedGroup. -->
        <Panel IsVisible="{Binding HasSelectedGroup}">
        <Grid RowDefinitions="*,Auto" DataContext="{Binding SelectedGroup}">
          <ScrollViewer Grid.Row="0" HorizontalScrollBarVisibility="Disabled">
            <StackPanel x:DataType="vm:PlanMapGroupViewModel" Spacing="12" Margin="16,12,16,12">
              <StackPanel Spacing="2">
                <TextBlock Classes="v2-heading-2" Text="{Binding MapLabel}" AutomationProperties.HeadingLevel="2"
                           TextTrimming="CharacterEllipsis" />
                <TextBlock Classes="v2-label" Text="{Binding Summary}" />
              </StackPanel>
              <Border Classes="v2-plan-section v2-plan-accent">
                <StackPanel Spacing="10">
                  <TextBlock Classes="v2-heading-3" Text="Quests" />
                  <ItemsControl ItemsSource="{Binding Quests}">
                    <ItemsControl.ItemsPanel>
                      <ItemsPanelTemplate><StackPanel Spacing="8" /></ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                      <DataTemplate x:DataType="vm:PlanQuestSummaryViewModel">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="10">
                          <Viewbox Width="18" Height="18" VerticalAlignment="Top" Margin="0,2,0,0">
                            <Path Classes="v2-icon v2-icon-muted" Data="{DynamicResource V2.Icon.Clipboard}" />
                          </Viewbox>
                          <StackPanel Grid.Column="1" Spacing="1">
                            <TextBlock Classes="v2-body" Text="{Binding Name}" TextWrapping="Wrap" />
                            <TextBlock Classes="v2-label" Text="{Binding TraderLabel}" />
                          </StackPanel>
                          <TextBlock Grid.Column="2" Classes="v2-label" Text="{Binding ObjectivesLabel}" VerticalAlignment="Top"
                                     Margin="0,2,0,0" />
                        </Grid>
                      </DataTemplate>
                    </ItemsControl.ItemTemplate>
                  </ItemsControl>
                </StackPanel>
              </Border>
              <!-- What the objectives ask for, where the catalog says: found in raid or hand in. -->
              <Border Classes="v2-plan-section" IsVisible="{Binding HasItemObjectives}">
                <StackPanel Spacing="8">
                  <TextBlock Classes="v2-heading-3" Text="Items" />
                  <Grid ColumnDefinitions="*,Auto">
                    <TextBlock Classes="v2-body" Text="Found in raid" />
                    <TextBlock Grid.Column="1" Classes="v2-body" FontWeight="SemiBold" Text="{Binding FindInRaidCount}" />
                  </Grid>
                  <Grid ColumnDefinitions="*,Auto">
                    <TextBlock Classes="v2-body" Text="Hand in" />
                    <TextBlock Grid.Column="1" Classes="v2-body" FontWeight="SemiBold" Text="{Binding HandInCount}" />
                  </Grid>
                </StackPanel>
              </Border>
            </StackPanel>
          </ScrollViewer>
          <StackPanel Grid.Row="1" x:DataType="vm:PlanMapGroupViewModel" Spacing="8" Margin="16,0,16,16">
            <Button Classes="v2-target v2-plan-primary" Command="{Binding OpenInRaidCommand}"
                    IsVisible="{Binding CanOpenInRaid}"
                    AutomationProperties.AutomationId="v2-plan-open-in-raid"
                    AutomationProperties.Name="Open in Raid">
              <StackPanel Orientation="Horizontal" Spacing="12">
                <Viewbox Width="20" Height="20">
                  <Path Classes="v2-icon" Data="{DynamicResource V2.Icon.Play}" />
                </Viewbox>
                <TextBlock Text="Open in Raid" VerticalAlignment="Center" />
              </StackPanel>
            </Button>
          </StackPanel>
        </Grid>
        </Panel>
      </Panel>
    </Border>
  </Grid>
</UserControl>
