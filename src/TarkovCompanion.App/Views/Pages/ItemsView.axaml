<UserControl x:Class="TarkovCompanion.App.Views.Pages.ItemsView"
             x:DataType="vm:ItemsPageViewModel"
             xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:TarkovCompanion.App.ViewModels">
  <Grid RowDefinitions="Auto,Auto,*" RowSpacing="12">
    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
      <TextBox Text="{Binding SearchQuery, Mode=TwoWay}"
               PlaceholderText="Search local item name or short name" />
      <Button Grid.Column="1" Classes="primary" Command="{Binding SearchCommand}" Content="Search local cache" />
    </Grid>
    <Border Grid.Row="1" Classes="inset" Padding="10">
      <TextBlock Text="{Binding SearchStatus}" TextWrapping="Wrap" />
    </Border>
    <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Disabled">
      <ItemsControl ItemsSource="{Binding Results}">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <WrapPanel ItemWidth="320" />
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
          <DataTemplate x:DataType="vm:ItemSearchResultViewModel">
            <Border Classes="panel" Width="308" Margin="0,0,10,10" Padding="13">
              <StackPanel Spacing="6">
                <Grid ColumnDefinitions="*,Auto">
                  <TextBlock FontWeight="SemiBold" Text="{Binding Name}" />
                  <TextBlock Grid.Column="1" Classes="label" Text="{Binding ShortName}" />
                </Grid>
                <TextBlock Classes="label" Text="{Binding Category}" />
                <TextBlock Classes="label" Text="{Binding Dimensions}" />
                <TextBlock Classes="metric ochre" FontSize="18" Text="{Binding Price}" TextWrapping="Wrap" />
                <TextBlock Text="{Binding PriceSource}" />
                <TextBlock Classes="label" Text="{Binding Match}" TextWrapping="Wrap" />
                <TextBlock Classes="label" Text="{Binding Updated}" TextWrapping="Wrap" />
              </StackPanel>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>
  </Grid>
</UserControl>
