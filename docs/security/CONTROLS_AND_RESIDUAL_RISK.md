# Controls and residual-risk register

Evidence tiers are defined in `METHODOLOGY.md`. Every row in this integrated wave-2 register is
**Reviewed**: implementation/configuration and named test source were read, but this docs pass did
not execute an exploit, penetration test, build, or product test. A later exact-head CI result can
be cited as automated evidence only for the checks it actually runs; workflow configuration and an
unobserved test name are not test evidence.

Issue #280 refreshed the two update-channel rows against its current source. They remain
**Reviewed**, not deployed or CI-proven: the private feed and protected environments do not yet
exist, and the desktop consumer's composition/state transaction belongs to #294/#270.

Every open row has an explicit release disposition. **Mitigate** is required work; **Defer** means
the named later design/review owns it and the row remains open; **Accept** records a bounded
residual risk. No High/Critical row below is silently accepted.

## Open findings

| ID | Severity | Abuse case(s) | Control today | Evidence | Residual risk | Release disposition |
| --- | --- | --- | --- | --- | --- | --- |
| RISK-RELAY-NO-RATE-LIMIT | **Medium** | ABUSE-RELAY-PUBLIC-ENDPOINT-DOS | Catalog paths/modes are allowlisted and catalog snapshots are cached; no application request-rate limit exists on `/catalog`, `/landmarks`, `/search`, or group-state routes. Cloudflare may front one deployed hostname but is not an application control and the LAN endpoint bypasses it. | Reviewed: `Program.cs`, `CatalogMirror.cs`, `RelayAccess.cs` | Anonymous callers can consume process/network capacity. Catalog upstream amplification is bounded by the small allowlist/cache, but repeated relay search/serialization and concurrent traffic remain unbounded by caller. | **Mitigate before v2 increases relay reliance.** Add per-IP/global concurrency and request limits with bounded queues and observable rejections. Owner: #304/#310 relay/gateway work. |
| RISK-RELAY-KEY-BRUTEFORCE | **High** | ABUSE-RELAY-WEAK-KEY-GUESS | Syntax accepts any non-whitespace key whose trimmed length is at least eight and raw length is at most 128. A closed relay allowlists room hashes, but neither open nor closed mode limits attempts at an active key. Registration restricts which room hashes exist; it does not authenticate the person supplying a registered key. | Reviewed: `GroupKey.cs`, `RelayAccess.cs`, `GroupRoomRegistry.cs`, `Program.cs` | Human-chosen low-entropy keys can be tested remotely. A successful guess grants full room read/write and display-name impersonation. Open mode exposes a content oracle (empty versus live room); closed mode exposes a registered-hash oracle (403 versus allowed). | **Mitigate.** Generate high-entropy defaults, provide entropy/rotation UX, rate-limit failed and successful room probes, and replace the reusable shared credential with scoped pairing/session credentials in #304. Release owner: #304/#310; remains open for v2. |
| RISK-RELAY-CLIENT-TRUST | **Medium** | ABUSE-RELAY-STALE-FRESHNESS | `sinceSeconds` is server-derived, but `positionAge` and all gameplay state are client-reported. The UI can show age/provenance; the relay has no independent game evidence. | Reviewed: `GroupRooms.cs`, `GroupContracts.cs` | A member can repeatedly publish a stale position as fresh, affecting every recipient in the room; three-minute expiry only removes a member who stops publishing and does not bound active fabrication. | **Accept for the current advisory relay. Owner: #304.** Preserve explicit source/age display; do not recast client claims as server-verified facts in v2. |
| RISK-RELAY-MARK-CAP | **Medium** | ABUSE-RELAY-WAYPOINT-FLOOD, ABUSE-RELAY-CROSS-ROOM-MARK-GROWTH | Per-room caps are 60 waypoints and 30 pings; insertion evicts the oldest. Pings are omitted/removed after 45 seconds on room read or later ping insertion. The seven-day waypoint filter runs only during load/restart. `_rooms` has no relay-wide cap, and every waypoint write serializes all non-empty rooms. | Reviewed: `GroupMarks.cs`, `Program.cs`, `GroupKey.cs` | A credential holder can replace one room's legitimate plan. On an open relay, rotating invented keys creates unbounded in-memory rooms and persistent waypoint sets; old waypoints remain live until a restart loads/prunes them. | **Mitigate before v2.** Add a global room/mark/serialized-byte bound, live expiry and removal of empty rooms, write-cost limits, and caller-level throttling. Owner: #304/#310. Per-device/member sub-limits remain necessary for within-room abuse. |
| RISK-RELAY-IDENTITY | **High** | ABUSE-RELAY-NAME-COLLISION, ABUSE-RELAY-LEAVE-WRONG-NAME | Current `GroupSessionService` remembers the identity it published when withdrawing. The relay still keys members by caller-supplied display name with last-write-wins semantics and no member session identity. | Reviewed: `GroupSessionService.cs`, `GroupRooms.cs` | Any room credential holder can impersonate another display name and replace its marker without detection. Rename/withdraw correctness in the stock client does not protect against another client. | **Mitigate in the paired-device protocol.** Bind writes to scoped member/device identities while keeping display names mutable. Owner: #304; remains open for v2. |
| RISK-RELAY-KEY-DISCLOSURE | **High** | ABUSE-RELAY-PLAINTEXT-KEY, ABUSE-RELAY-CLEARTEXT-CREDENTIAL | Public-host desktop configuration requires HTTPS, but local/private HTTP is explicitly accepted; tablet/admin fetches inherit their page scheme, and the deployed origin binds HTTP on every interface. Under HTTPS the receiving relay still parses the plaintext group key before hashing it. Stock source does not intentionally log the plaintext. | Reviewed: `GroupSharing.cs`, `GroupSessionService.cs`, `Tablet/index.html`, `Admin/index.html`, `Program.cs`, `GroupKey.cs`, `deploy/group-server/tarkov-group.service` | An on-path LAN actor can steal/alter reusable group or admin credentials and content on direct HTTP. A malicious, compelled, instrumented, or compromised receiving relay can retain group keys even under HTTPS. Hash-only persistence is neither transport protection nor relay blindness. | **Mitigate in #304/#310.** Require an authenticated confidential transport for every non-loopback route, prevent/detect unsafe tablet/admin origins, issue revocable scoped device/session credentials, and document which endpoint can recover each secret. Review any relay-blind proof as a protocol, not as hashing after receipt. |
| RISK-GROUP-KEY-LOCAL-EXPOSURE | **Medium** | ABUSE-GROUP-KEY-LOCAL-RECOVERY | None for A-5 at rest: desktop `Config/group.json` and tablet `localStorage` hold plaintext. Rotation is global; there is no device-specific revocation. DPAPI protects the unrelated TarkovTracker token only. | Reviewed: `JsonFileGroupSettingsStore.cs`, `Tablet/index.html`, `WindowsDpapiSecretStore.cs` | Same-user malware, a lost/unlocked tablet, or a later browser user recovers full room authority until all members rotate the shared key. | **Mitigate in the v2 pairing design.** Issue revocable device credentials, protect desktop material with an appropriate OS store, minimize browser persistence, and expose device/rotation state in Setup/Admin. Owner: #304. |
| RISK-RELAY-OBSERVED-DATA-POLICY | **High** | ABUSE-RELAY-OBSERVED-DATA-POLICY | The relay prunes `Observed` entries against names currently present in the room on ingress and egress. That limits recipients but does not satisfy `docs/SAFETY.md` lines 32–35, which currently prohibit transmitting other players' log-derived data at all. The desktop sends the field whenever sharing is enabled, independently of the own-loadout switch. | Reviewed: `GroupSessionService.Describe`, `GroupContracts.cs`, `GroupRooms.PruneObserved`, `docs/SAFETY.md` | Current source and current normative safety documentation disagree. Treating room membership as implicit consent would silently relax a rule this worktree is not authorized to change. | **Mitigate; release-blocking until reconciled. Owner: #310.** Stop transmitting the field under the current contract, or obtain an explicit separately reviewed policy/product change before designing a consent control; this docs PR records rather than resolves the defect. |
| RISK-CATALOG-INTEGRITY | **Medium** | ABUSE-CATALOG-UPSTREAM-TAMPER, ABUSE-CATALOG-STALE-FALLBACK | HTTPS authenticates transport. The mirror validates a minimal JSON shape and hashes received bytes for ETag identity. That hash is not an upstream signature. After expiry, refresh failure silently returns held bytes; the response does not expose the failed refresh or fetched age. | Reviewed: `CatalogMirror.cs`, catalog endpoint in `Program.cs`, `TarkovDevJsonClient.cs` | Manipulated or stale price/quest/map data can drive incorrect advisory decisions while appearing to be a normal successful mirror response. Direct upstream clients share authenticity risk; silent stale fallback is mirror-specific. | **Defer with required v2 control.** #311/data-ingestion work must carry source, fetched/data-through time, stale/degraded status, and snapshot/model version to UI. Consider independently signed/versioned snapshots if an authoritative mechanism becomes available. |
| RISK-UPDATE-CHANNEL-TRUST | **High** | ABUSE-UPDATE-CHANNEL-DOWNGRADE | Issue #280 separates verification from publication, fetches producer artifacts by GitHub's recorded digests, re-runs the release-time vulnerability/license/secret gates, reconciles all binary/data/model identities, and signs every file, manifest and create-once ring decision with `publish.yml`'s main-branch identity. Builds must be immutable in a separate private/internal feed. Tools/actions are content-pinned. The relay verifies this chain with independent trust-root/history/floor policy; the old public `GithubSource` path is removed, and the uncomposed desktop gateway fails closed while its bounded authenticated consumer awaits #294/#270. | Reviewed: `docs/RELEASES.md`, ADR 0011, `publish.yml`, `windows-verify.yml`, `scripts/release/`, `SignedReleaseFeedConsumer.cs`, `AuthenticatedGitHubReleaseFeed.cs`, `CosignReleaseSignatureVerifier.cs`, `VelopackUpdateGateway.cs`, `tarkov-group-update.sh` | Feed control can freeze delivery and a history-free consumer needs an out-of-band floor. Protected-main/publisher-environment compromise remains signing authority. Required GitHub environments/feed settings are absent, exact-head CI and the first live feed/host run are not yet evidence, and desktop activation/history are intentionally not composed. | **Mitigate before `V2_RELEASES_ENABLED=true`. Owner: #280 for exact-head CI and first-feed/operator evidence; #294/#270 for desktop activation and durable state.** Do not enable releases or claim desktop in-app coverage until those gates are complete. |
| RISK-DPAPI-SAMEUSER | **Medium** | ABUSE-DPAPI-SAME-USER-MALWARE | TarkovTracker token uses DPAPI `CurrentUser`; the integration is optional, read-only, canonical-host, and user initiated. | Reviewed: `WindowsDpapiSecretStore.cs`, `TarkovTrackerApiClient.cs` | Malware already running as the same Windows user can recover the read-only token. This is credential exposure after the meaningful same-user prerequisite, matching the methodology's Medium threshold. | **Accept. Owner: #317.** This matches DPAPI's boundary and the token's restricted capability. Do not generalize this acceptance to plaintext A-5. |
| RISK-ANTICHEAT-REVIEW-DISCIPLINE | **Medium** | ABUSE-ANTICHEAT-UNREVIEWED-EVIDENCE-SURFACE | Permanent rules, platform layering, current source review, the merged #264 typed evidence/wire contract, and `audit-safety.sh` lexical patterns. The scanner catches named APIs/packages, click-through windows, formatted topmost+layered flags, adjacent-statement game-window reparenting, scanner errors, and scan-root symlinks. It cannot prove intent or catch every renamed/dynamic mechanism, and a valid ordinary topmost companion is not itself an overlay. | Reviewed: `AGENTS.md`, `docs/SAFETY.md`, `docs/V2_CONTRACT.md`, ADR 0008, `scripts/audit-safety.sh`, merged V2 contract types/tests, current App views/platform source | A future feature can still violate automation, live-enemy, overlay, or evidence-honesty rules without a named lexical signal or by bypassing the generic contract at a feature-specific boundary. | **Mitigate continuously.** #305/#311 and every new capture/evidence surface must add deterministic feature tests and explicit security review; expand static rules only where a precise low-false-positive check exists. Breaking an immutable fixture is Critical even though this process-gap finding is Medium. |
| RISK-REPORT-RATE-LIMIT | **Medium** | ABUSE-ADMIN-REPORT-FLOOD | Kestrel caps all request bodies at 32 KiB. `ProblemReports` records at most three attempts per derived room/hour, but room is derived from attacker-controlled key and `_filed` never removes room buckets. Files are retained without a global count, age, or disk quota. | Reviewed: `Program.cs`, `ProblemReports.cs`, `GroupKey.cs` | On an open relay, rotating keys bypasses the limiter and grows both in-memory buckets and `reports/*.md` until process/disk limits intervene. The 32 KiB body bound limits each write, not aggregate writes. | **Mitigate before v2.** Add per-IP and process-wide limits, bounded/expiring counters, report TTL/count/disk quota, and lifecycle/audit behavior in #310. This row is open; the former closed classification was incorrect. |
| RISK-REPORT-REDACTION | **High** | ABUSE-REPORT-INCOMPLETE-REDACTION | `SupportBundle` omits game logs, group keys, and screenshots, masks digits in the selected screenshot names, and calls `Redact` on its app-log tail. It nevertheless inserts `Observation.Detail` verbatim, and `Redact` is not applied to the assembled bundle or designed to remove raw roots, screenshot filenames, or coordinates from every field. `/report` accepts the body and `ProblemReports` writes it unchanged. | Reviewed: `SupportBundle.cs`, `RaidObservationService.cs`, `FileLoggerProvider.cs`, `Program.cs`, `ProblemReports.cs`, `docs/SAFETY.md` | A normal user-triggered report can export paths and location-bearing details to a separately operated relay, contrary to the current safety contract. The relay's arbitrary-body persistence means its storage control cannot repair an incomplete client bundle. | **Mitigate; release-blocking.** #281 must define/test exact-path and coordinate absence by default; #310 must allowlist/redact the complete assembled bundle, provide report preview, exclude coordinates/logs by default, and test the transmitted body plus persisted relay body rather than `Redact` alone. This docs PR records the defect and does not change source or policy. |
| RISK-SCREENSHOT-RETENTION-DEFAULT | **Low** | ABUSE-SCREENSHOT-RETENTION-SURPRISE | Cleanup matches game screenshot names only, retains the newest, skips cloud-only placeholders, and uses the recycle bin. However, missing/unreadable settings select enabled 24-hour retention rather than off. GDI capture bytes are a separate path and are never persisted. | Reviewed: `ScreenshotRetention.cs`, `JsonFileScreenshotRetentionStore.cs`, `GdiScreenCaptureService.cs`, `ScanUseCase.cs` | A player who did not understand or could not load the setting can have older game screenshots moved. Recovery and selection controls bound impact, but a default file mutation needs explicit consent/status UX. | **Defer to #309 with a required decision.** Reconfirm default/upgrade behavior, expose effective cleanup state and last action, retain recoverability, and test capture non-persistence separately from game-file retention. |
| RISK-LOCAL-IMPORT-EXPORT-INTEGRITY | **Medium** | ABUSE-LOCAL-IMPORT-REPLAY, ABUSE-LOCAL-EXPORT-DISCLOSURE, ABUSE-PROFILE-SCHEMA-AMPLIFICATION, ABUSE-CSV-FORMULA-INJECTION | Profile JSON is size/schema/value bounded and atomically stored. Quest JSON is size/depth/count/string bounded, canonical-checksummed, and previewed before confirmation. Raid history exports JSON/CSV without importing. | Reviewed: `JsonFilePlayerProfileService.cs`, `ProjectQuestProgressJson.cs`, `QuestProgressExchangeService.cs`, `SqliteRaidHistoryService.cs`, `RaidHistoryOutbox.cs` | A valid older profile envelope can replace newer local state; bounded profile JSON still lacks collection/string/duplicate policy; a malicious author can recompute an in-document checksum. Raid exports disclose exact identifiers/times/notes and CSV cells remain formula-active. | **Defer to #315 with #317 review.** Add explicit freshness/replay/schema decisions, preserve preview/confirm and atomicity, test hostile bounded documents, neutralize spreadsheet-active CSV values, and disclose/minimize each export's content. |

### Wave-2 verification for existing canonical risks

| Risk | Wave-2 audit evidence | GitHub-issue owner and exact verification before disposition changes |
| --- | --- | --- |
| RISK-RELAY-NO-RATE-LIMIT | RELAY-OPS-01 | #310: drive per-IP/global limit+1 public and group requests through direct-LAN and fronted routes; assert bounded queue/concurrency, 429, recovery, and observable rejection. |
| RISK-RELAY-KEY-BRUTEFORCE | RELAY-AUTH-02; crypto audit | #304/#310: test generic room denials, generated/user-key policy, valid/invalid per-IP and room backoff, rotation/revoke, then record an authorized staging rate-limit run. |
| RISK-RELAY-CLIENT-TRUST | RELAY-TRUST-01 | #304: fabricate age/state and alter a valid relay reply; assert sender/transport/server-receipt provenance stays visible and is never presented as verified detection. |
| RISK-RELAY-MARK-CAP | RELAY-STATE-03/04 | #304/#310: test actor/global room, mark and serialized-byte quotas, idempotency/revision, live TTL, empty-room removal, saturation denial, and bounded write work. |
| RISK-RELAY-IDENTITY | RELAY-AUTH-01; crypto replay trace | #304: distinct session credentials, forged-name/leave/mark rejection, authenticated rename, delayed delete, and single-device revoke without global rekey. |
| RISK-RELAY-KEY-DISCLOSURE | RELAY-AUTH-03; crypto audit | #304/#310: reject every non-loopback HTTP and unsafe browser origin, prove scoped revocable credentials, document endpoint disclosure, and run an authorized staging plaintext-negative test. |
| RISK-GROUP-KEY-LOCAL-EXPOSURE | Crypto audit | #304: protected desktop device records, bounded browser persistence/clear, copied-key rejection after revoke, and one-device revoke without rekeying peers. |
| RISK-RELAY-OBSERVED-DATA-POLICY | Relay reconciliation | #310: inspect exact desktop payload and persisted/relayed state under every consent switch; assert no prohibited log-derived third-party data crosses under the current Safety contract. |
| RISK-CATALOG-INTEGRITY | Serialization audit | #311: tampered/stale upstream snapshots with source/fetched/data-through/degraded/version propagation; assert independent authenticity if adopted and no silent fresh presentation. |
| RISK-UPDATE-CHANNEL-TRUST | Crypto audit; WIN-012; issue #280 implementation review | #280: exact-head License lock, CI and Windows verification; capture required GitHub controls; publish once to the real private immutable feed; verify signature/provenance/ring artifacts and a relay install/rollback. #294/#270: compose and exercise the desktop binary/data/model activation and durable replay/LKG state on Windows. |
| RISK-DPAPI-SAMEUSER | Crypto audit | #317: add Windows save/load/delete/failure tests and record exact CI; preserve canonical-origin/redirect/redaction tests without claiming same-user secrecy. |
| RISK-ANTICHEAT-REVIEW-DISCIPLINE | WIN-014 | #317: run safety audit on exact merge head and perform focused semantic/source/test review for every new P/Invoke, native dependency, capture, input, window, hotkey, placement, or evidence surface. |
| RISK-REPORT-RATE-LIMIT | DIAG-05; RELAY-OPS-01 | #310: align request-byte contract and test global/per-IP/per-room quotas, expiring counters, disk pressure, atomic write, restart, count/byte/TTL deletion, duplicate/processed lifecycle. |
| RISK-REPORT-REDACTION | DIAG-01/02/06 | #281/#310: hostile full runtime/log corpus through preview, byte-exact send, and relay persistence; assert no path, coordinate, identity, token/key, game log/screenshot, newline, or Markdown-fence injection. |
| RISK-SCREENSHOT-RETENTION-DEFAULT | DIAG-07; PERS-04 | #309: corrupt/missing/denied settings after saved-off state plus first-run migration; assert default-off consent, preview/ledger, exact roots/files, no recycle, and restore/race/reparse behavior. |
| RISK-LOCAL-IMPORT-EXPORT-INTEGRITY | SER-PROFILE-SCHEMA-AMPLIFICATION, SER-CSV-FORMULA-INJECTION, SER-QUEST-IMPORT-BOUNDS; PERS-08 | #315: depth/count/string/duplicate/unknown compatibility at limit+1, unchanged state on rejection, checksum replay/preview, spreadsheet-active cells neutralized, and disclosed/minimized export bytes. |

## Wave-2 canonical additions

These rows are the canonical homes for distinct findings discovered by the six wave-2 audit
lanes. Audit IDs are evidence locators, not parallel risk IDs. Every implementation statement is
**Reviewed** only; named tests were found in source but no test run was observed. “Exact
verification” is work required before a disposition may change, and this review is never proof
that mitigation exists.

| ID | Severity | Abuse case(s) | Audit evidence and control today | Residual risk | Disposition / GitHub-issue owner | Exact verification required |
| --- | --- | --- | --- | --- | --- | --- |
| RISK-EXTERNAL-DATA-BOUNDS | **High** | ABUSE-DESKTOP-CATALOG-RESOURCE-EXHAUSTION, ABUSE-RELAY-CATALOG-RESOURCE-EXHAUSTION, ABUSE-RELAY-LANDMARK-RESOURCE-EXHAUSTION | **Reviewed:** SER-HTTP-UNBOUNDED-CATALOG, SER-RELAY-CATALOG-UNBOUNDED, SER-RELAY-LANDMARKS-UNBOUNDED, SER-V2-CONTRACT-TRANSPORT-PRECONDITION. Timeouts, allowlists, refresh gates, caches, and System.Text.Json's default depth 64 do not bound accepted/decompressed bytes, aggregate counts, or strings. | One accepted catalog or landmarks response can allocate multiple body/DOM/gzip/materialized copies and exhaust the desktop or public relay. | **Mitigate before v2 ingestion/relay expansion. Owner: #311 with #310.** | Stream unknown-length and compressed limit/limit+1 bodies; assert bounded rejection before cache mutation, bounded gzip/materialization, honest stale/degraded fallback, and byte/count/string/depth limits on every V2 reader. |
| RISK-MAP-ASSET-ORIGIN | **Medium** | ABUSE-MAP-ASSET-REDIRECT | **Reviewed:** SER-MAP-ASSET-REDIRECT-ORIGIN. Initial HTTPS host is allowlisted and bytes are capped/hashed, but the final redirected URI is not revalidated. | Content from an unapproved origin can be cached and shown with the initial provider's attribution. | **Mitigate. Owner: #311.** | Redirect an approved initial URL to approved and unapproved final origins; reject the latter before asset/metadata write and assert final URI/content-type attribution. |
| RISK-MAP-CATALOG-CONFIGURATION | **Low** | ABUSE-MAP-CATALOG-CONFIGURATION | **Reviewed:** SER-MAP-CATALOG-TRUST. Product composition supplies a fixed default and the reader caps/parses/caches, but its options seam accepts any absolute HTTPS URI and does not revalidate final redirects. | A future configuration surface could become an arbitrary content origin without a new policy guard. | **Defer; no runtime configuration surface may ship first. Owner: #311.** | Reject non-approved initial/final URIs through the options seam unless a reviewed source/provenance policy explicitly allows them. |
| RISK-PERSISTENCE-SCHEMA-COMPATIBILITY | **High** | ABUSE-PERSISTENCE-NEWER-SCHEMA | **Reviewed:** SER-MIGRATION-NEWER-SCHEMA and PERS-01. Per-migration transactions/backups exist, but unknown recorded versions do not fence later known migration writes and resources have no checksum. | A downgraded/sidegraded build can alter a newer schema it does not understand, causing divergence, startup failure, or data loss. | **Mitigate; blocks v2 rollback claims. Owner: #270 with #314.** | Open a future-version fixture with a missing known migration; assert read-only/recovery refusal, no SQL/data change, stable diagnostic, and checksum mismatch refusal if resource checksums are adopted. |
| RISK-PERSISTENCE-RECOVERY | **Medium** | ABUSE-PERSISTENCE-RECOVERY-FAILURE | **Reviewed:** PERS-02 and PERS-11. WAL, transactions, and `VACUUM INTO` help ordinary failures; backup errors are suppressed, prune order trusts timestamps, readers disagree between throwing and invented defaults, and no integrity/quarantine/restore flow exists. | A destructive migration can proceed without a verified backup; corruption can hide chronology or remove whole views without source-honest recovery state. | **Mitigate. Owner: #270.** | Inject full disk, permissions, interruption, malformed rows, and corrupt DB; assert migration fencing, deterministic quarantine, no invented evidence, verified backup selection/restore, and visible recovery state. |
| RISK-PROFILE-IMPORT-STATE | **High** | ABUSE-PROFILE-CONTEXT-REPLACEMENT | **Reviewed:** PERS-03. Profile import validates and atomically replaces one JSON file, but performs no freshness/scope/context/revision comparison and cannot atomically switch independently scoped SQLite state. | A stale or wrong-context import can publish inconsistent active identity/history/progress and lacks quarantine or restore. | **Mitigate. Owner: #269 with #270.** | Seed active profile/context data, import older/wrong-mode/wrong-generation/malformed state, and assert explicit conflict/quarantine, no pre-confirm mutation, recoverable storage, and exact-scope presentation. |
| RISK-LOCAL-FILE-IDENTITY | **Medium** | ABUSE-LOCAL-FILE-PATH-SUBSTITUTION | **Reviewed:** PERS-05, PERS-10, and WIN-007. Sibling temp/move, reparse prechecks, DPAPI, and recycle-bin selection exist, but fixed scratch names, instance-local gates, pathname rechecks, and writable roots leave cross-process/TOCTOU substitution. | A same-user actor can redirect or replace a JSON, secret, export, or screenshot target after validation, affecting confidentiality/integrity/availability. | **Mitigate. Owner: #270; screenshot-specific work #309.** | Deterministic barrier tests for concurrent writers, scratch collision, junction/reparse/root swap, candidate identity swap, interruption, and final-handle containment; assert no outside-policy target changes. |
| RISK-LOCAL-STATE-RECOVERY | **Low** | ABUSE-LOCAL-JSON-SILENT-RESET | **Reviewed:** PERS-06 and PERS-12. Small size/path/shape bounds exist, but malformed preferences become empty and invalid event definitions disappear; later writes can overwrite recoverable bytes without a rejection ledger. | Low-value local choices/evidence can be silently lost or shadowed, making “absent” indistinguishable from “rejected.” | **Mitigate/defer to shared recovery primitives. Owner: #270 with #315.** | Corrupt, duplicate, oversize, deny, and interrupt each store; assert quarantine/rejected-evidence state, deterministic duplicate policy, retained valid siblings, and no temp residue. |
| RISK-RAID-HISTORY-DURABILITY | **Medium** | ABUSE-RAID-HISTORY-DROPPED-EXPORT | **Reviewed:** PERS-07. The ordered bounded outbox retries five times and disposal drains completed work, but a persistent error drops an event and export does not flush despite an available barrier. | History/export can look complete while silently missing failed or still-queued observations. | **Mitigate. Owner: #270 with #315.** | Force five failures followed by success; assert durable gap status, ordered later data, and export that flushes or explicitly reports pending/lost entries. |
| RISK-CONTEXT-ISOLATION | **High** | ABUSE-CONTEXT-HISTORY-CROSSOVER | **Reviewed:** PERS-09. Raid writes include profile/mode, but list/export reads are global, trails filter only map, and rows lack wipe generation. | Data and evidence from another profile/mode/generation can appear under the active context or in an export without comparison labeling. | **Mitigate before multi-context v2 release. Owner: #269, #270, and #315.** | Seed identical maps across profiles, modes, and generations; switch rapidly and assert exact active scope for list/trail/export unless a separately labeled comparison mode is selected. |
| RISK-ERROR-DISCLOSURE | **Medium** | ABUSE-ERROR-DETAIL-DISCLOSURE | **Reviewed:** DIAG-03. Failures stay visible, but raw `exception.Message` reaches UI/console and may flow into copied/sent diagnostics. | Paths, tokens, endpoints, or attacker-crafted structure can be disclosed and then exported. | **Mitigate. Owner: #281.** | Inject path/key/newline-bearing exceptions at each UI boundary; assert stable actionable safe copy and a separately allowlisted diagnostic event. |
| RISK-DEGRADED-STATE-INTEGRITY | **Medium** | ABUSE-DEGRADED-STATE-HIDDEN | **Reviewed:** DIAG-04. Time-bounded shutdown and restart loops aid availability, but dispatcher exceptions, withdrawal failures, and abandoned work can be swallowed without durable visible state. | The application can continue with inconsistent UI/relay state while the player cannot distinguish degraded truth from success. | **Mitigate. Owner: #281.** | Exercise partial dispatcher mutation, failed withdrawal, and shutdown timeout; assert supervised state, sanitized evidence, and visible degraded/abandoned status. |
| RISK-CAPTURE-BUFFER-LIFETIME | **Medium** | ABUSE-CAPTURE-BUFFER-RESIDUE | **Reviewed:** DIAG-08. Current persistence stores metadata, not pixels, and image size is capped; decoded managed buffers lack an explicit dispose/zero/ownership lifetime. | Cancellation/error or future debug code can retain full-image/crop pixels longer than the documented scan session. | **Mitigate. Owner: #271 with #309.** | Instrument success/cancel/error paths for released references and no pixel persistence; prove opt-in Debug Capture quota, expiry, delete, and revoke behavior without claiming GC secure erasure. |
| RISK-DIAGNOSTIC-CHANNEL-RETENTION | **Medium** | ABUSE-DIAGNOSTIC-CHANNEL-RETENTION | **Reviewed:** DIAG-09 and the crypto audit's `windows-smoke.ps1` trace. Developer mode/token/command grammar constrain capability; command JSON contains the token until normal post-response deletion, responses persist, and no byte/count/TTL bound exists. | A crash retains a token-bearing file; a same-user developer process can exhaust the channel directory. Production default remains disabled. | **Defer with explicit developer-boundary hardening. Owner: #281.** | Assert restrictive root ACL/ownership, bounded file count/bytes, command/response TTL cleanup, crash recovery, token rotation/expiry, and malformed-file flood behavior. |
| RISK-OUTBOUND-INVENTORY | **Low** | ABUSE-OUTBOUND-SURFACE-DRIFT | **Reviewed:** DIAG-10. No analytics SDK was found; offline composition rejects HTTP and group/report traffic is feature configured. No user-visible complete outbound inventory or no-unsolicited-request semantic gate exists. | A future collector or route can expand outbound behavior without inspectable consent/revocation or honest degraded state. | **Mitigate before adding telemetry/collection. Owner: #281.** | Execute a network allowlist/no-unsolicited-request test per mode and test default-off consent, revoke, export, delete, and offline truth for any collector. |
| RISK-ADMIN-KEY-BRUTEFORCE | **High** | ABUSE-ADMIN-KEY-GUESS | **Reviewed:** crypto audit and `RelayAdmin.cs`. One header and fixed-time equality are sound comparison controls; configured secrets have no entropy/minimum rule and requests have no throttling/backoff. | Guessing a weak admin key grants room registration/removal, update requests, and all retained report bodies. | **Mitigate before v2 operator use. Owner: #310.** | Enforce generated/high-entropy provisioning and rotation; test limit/limit+1 secrets, every protected route, generic failures, per-IP/global backoff, and an authorized staging rate-limit run. |
| RISK-GROUP-KEY-GENERATION-BIAS | **Low** | ABUSE-GROUP-KEY-GENERATION-BIAS | **Reviewed:** crypto audit and `GroupRoomRegistry.Generate`. Sixteen CSPRNG bytes map through `% 31`; output has 16 symbols plus three separators, about 79.27-bit alphabet ceiling and 77.28-bit min-entropy. | Ranking eight slightly likelier symbols reduces entropy but still leaves generated-key exhaustion impractical; weak user-selected keys and online throttling remain separate High risk. | **Accept for current generated keys; use unbiased generation in redesign. Owner: #278.** | Assert exact 31-symbol/16-symbol/three-separator shape; with deterministic bytes, prove rejection sampling or another unbiased mapping before claiming uniform output. |
| RISK-RELAY-REGISTRY-FAIL-OPEN | **High** | ABUSE-RELAY-REGISTRY-FAIL-OPEN | **Reviewed:** RELAY-AUTH-04. A nonempty registry closes the relay, but load exception clears all rooms and empty means open. | Corruption or unreadability silently changes authorization and permits invented keys/rooms. | **Mitigate; blocks v2 closed-relay claims. Owner: #310.** | Corrupt/truncate/deny `rooms.json`; assert last-known-good or explicit maintenance refusal, safe permissions, recovery alert, and no invented-room admission. |
| RISK-RELAY-ORDERING | **Medium** | ABUSE-RELAY-OUT-OF-ORDER-STATE | **Reviewed:** RELAY-STATE-01. Store locks serialize individual operations, but replies have no room/member revision or client sequence and member/mark snapshots are read separately. | Older replies and stale tablet mutations can overwrite newer presentation or resolve races only by arrival order. | **Mitigate. Owner: #304 with #276.** | Reverse deterministic replies and race state/mark create/clear/reach/delete; assert monotonic revision, idempotency/conflict, and atomic snapshot policy. |
| RISK-RELAY-CAPACITY | **Medium** | ABUSE-RELAY-SILENT-CAPACITY | **Reviewed:** RELAY-STATE-02. Per-room member/room caps exist, but new-member count/check/add is not atomic and non-admission still returns ordinary 200 state. | A sender can believe it joined when it did not; concurrent admission can exceed or inconsistently enforce capacity. | **Mitigate. Owner: #310.** | Test exactly 16/17 members, refresh at cap, concurrent admission, and explicit bounded 409/429 semantics with visible client state. |
| RISK-RELAY-BROWSER-HARDENING | **Medium** | ABUSE-RELAY-BROWSER-CONTEXT | **Reviewed:** RELAY-BROWSER-01. Current DOM writes use `textContent` and custom headers prevent a simple cross-origin form, but pages/APIs lack CSP, frame, referrer, permissions, cache, Origin, and Fetch Metadata policy. | Same-origin injection/framing can exercise stored bearer material; a future cookie auth change could add CSRF exposure silently. | **Mitigate. Owner: #304 with #310.** | Assert nonce/hash CSP, `frame-ancestors 'none'`, nosniff, referrer/permissions/no-store policy, Origin/Sec-Fetch enforcement, and CSRF token if cookie authority is introduced. |
| RISK-RELAY-UPDATE-STATE | **Medium** | ABUSE-RELAY-UPDATE-STALE-STAMP | **Reviewed:** issue #280 now journals the prior tree, deployment files and installed stamps; commits installed stamps only after signed identity health succeeds; restores on health/start/stop/deployment/commit failure or interruption; records refusal separately; and projects status from a root-owned directory that the relay cannot write. Fixture source covers each failure, SIGTERM, next-tick refusal and interrupted recovery. | No exact-head CI result or authorized staging-host run yet proves the fixture suite and systemd/filesystem assumptions together. The root update unit is intentionally powerful and has not been sandboxed on the deployed host. | **Mitigated in source; keep open until #280 integration evidence and the first staged relay update/rollback are recorded.** | Run exact-head release fixtures, then stage a signed good build and a wrong-identity/failed-health build on the real service; assert old tree/stamps/status, separate refusal, next tick, timer/path behavior and no relay-writable update state. |
| RISK-WINDOW-CAPTURE-IDENTITY | **Medium** | ABUSE-WINDOW-CAPTURE-HANDLE-SWAP | **Reviewed:** WIN-001. Process names and nonzero window/bounds are checked; PID/executable/HWND identity is not revalidated immediately before capture. | Same-user spoofing or normal handle recycling can capture unrelated visible pixels. | **Mitigate before production window targeting. Owner: #316.** | Fixture process-name/HWND reuse/hidden-child cases plus Windows VM replacement between discovery and capture; assert fail-closed PID/image revalidation. |
| RISK-CAPTURE-BOUNDS | **Medium** | ABUSE-CAPTURE-INTEGER-OVERFLOW | **Reviewed:** WIN-002 and WIN-010. Some stride/allocation checks exist, but rectangle sums and Windows OCR byte/scale calculations contain unchecked paths and desktop fallback lacks virtual-bound containment. | Malformed in-process dimensions can capture unintended visible pixels, throw natively, or exhaust allocation. | **Mitigate. Owner: #271 with #316.** | Property/fuzz limit tests across int extremes, formats, stride, regions, virtual desktop, OCR maximum size, coordinate casts, and cancellation; use checked/subtraction-based validation shared by providers. |
| RISK-WINDOW-NATIVE-LIFETIME | **Medium** | ABUSE-WINDOW-NATIVE-LIFETIME | **Reviewed:** WIN-003 and WIN-009. `finally`/`using` clean ordinary paths and Tesseract serializes recognition, but `HGDI_ERROR`, DPI restoration, and concurrent OCR dispose are not controlled. | Faults can leak GDI handles, alter worker DPI context, or use/dispose native engines concurrently. | **Mitigate. Owner: #271.** | Windows fault-injection and concurrent recognize/dispose/cancel loops; assert no handle growth, correct prior-object/DPI restoration, and quiesced engine disposal. |
| RISK-WINDOW-DPI | **Medium** | ABUSE-WINDOW-DPI-MISMATCH | **Reviewed:** WIN-004. Paths request per-monitor-v2 awareness and retain physical monitor bounds/scale, but no Windows run exercised cross-API coordinate semantics. | Capture and standalone placement can be offset/scaled under mixed DPI or negative origins. | **Defer to required Windows evidence. Owner: #316.** | Windows VM matrix at 100/125/150 percent, mixed monitors, negative origins, BGRA orientation, placement restore, and desktop fallback. |
| RISK-WATCHED-PATH-CONTAINMENT | **High** | ABUSE-WATCHED-PATH-REPARSE | **Reviewed:** WIN-005. Filename/extensions narrow inputs, but recursive log watch and screenshot decode do not retain canonical-root/final-handle containment across reparse/rename races. | A same-user actor can make the companion read non-EFT content, violating the narrow allowed-evidence boundary. | **Mitigate before configured paths are trusted. Owner: #316.** | Windows junction/symlink root and nested reparse, rename/replacement at open, inaccessible root, and final-handle identity/containment tests. |
| RISK-WATCHER-BOUNDS | **Low** | ABUSE-WATCHER-UNBOUNDED-SEEN-SET | **Reviewed:** WIN-006. Polling/retry and extension/order controls exist, but screenshot paths are marked seen before successful processing and retained without a bound. | Large/racy directories can consume memory/CPU or lose a retry while scan pixels remain nonpersistent. | **Accept temporarily; bound before broad path support. Owner: #316.** | Stress create/write/rename/delete/restart/replacement and large-directory cases; assert bounded identity tracking, retry readiness, and visible overflow/error state. |
| RISK-NATIVE-OCR-SUPPLY-CHAIN | **High** | ABUSE-NATIVE-OCR-LOAD-HIJACK | **Reviewed:** WIN-008. Model bytes are hashed and the resolved NuGet graph/content hashes are diff-gated, but no explicit native DLL search/resolver or Authenticode policy was found. | Writable search resolution or a compromised build/release can load attacker native code; model hashing is unrelated. | **Mitigate before signed public release. Owner: #317 with #314.** | Windows clean-profile, missing/transitive DLL, poisoned working/search directory, controlled resolver, and signed-artifact verification on exact head. |
| RISK-SHELL-LAYOUT-RECOVERY | **Low** | ABUSE-SHELL-LAYOUT-CORRUPTION | **Reviewed:** WIN-011. Size/scale/clamping and normal standalone-window controls exist, but coordinates are not fully finite/bounded before casts and coordinate spaces are not proven equivalent. | Corrupt/stale layout can make the companion hard to reach without creating an overlay or input path. | **Accept with hardening queued. Owner: #316.** | NaN/infinity/extreme/negative-origin/mixed-DPI/no-screen/unplug restore tests and retained no-overlay source review. |
| RISK-INSTALL-PRIVILEGE | **Medium** | ABUSE-INSTALL-ELEVATION | **Reviewed:** WIN-013. Install is designed for distinct per-user LocalAppData and CI checks data-root separation; token integrity, elevation policy, and temp/reparse behavior are not verified. | An elevated launch broadens any package/path defect beyond the intended user boundary. | **Mitigate with update work. Owner: #314 with #317.** | Standard/elevated token matrix, no-UAC-request assertion, temp/reparse/ACL cases, and no writes outside per-user install/data roots. |

## Closed findings

Closed means this pass read a control and found it sound for the narrowly stated regression case;
it does not claim an executed test.

| ID | Abuse case(s) | Control | Evidence | Why closed |
| --- | --- | --- | --- | --- |
| RISK-REPORT-TRAVERSAL | ABUSE-ADMIN-REPORT-REFERENCE-TRAVERSAL | `ProblemReports.Read` requires exactly 12 lowercase hexadecimal characters before using the value in a filename glob. | Reviewed: `ProblemReports.cs` | Validation precedes filesystem enumeration and excludes separators/dots. The illustrative fixture is not an automated test; add one during #310. |
| RISK-ADMIN-KEY-TIMING | ABUSE-ADMIN-KEY-TIMING | `RelayAdmin.IsAuthorised` uses `CryptographicOperations.FixedTimeEquals` for the provided and configured UTF-8 values. | Reviewed: `RelayAdmin.cs` | Prevents byte-by-byte early-exit comparison for equal-length candidates. Different lengths may remain distinguishable; that reveals at most secret length, not byte values, and should be retained as a regression-test caveat. No timing run was performed. |
| RISK-TARKOVTRACKER-REDIRECT | ABUSE-TARKOVTRACKER-REDIRECT | `AllowAutoRedirect = false` is set on the `HttpClientHandler`; requests are limited to the canonical HTTPS origin and two GET paths. | Reviewed: `TarkovTrackerApiClient.cs` | A redirect response is surfaced rather than followed with the bearer token. No live request was sent in this pass. |

Closed rows retain regression owners and are not execution evidence: #310 owns a traversal
fixture against every report-body read and direct configured/unconfigured/multiple-header admin
comparison tests; #315 owns the exact TarkovTracker redirect-with-credential case. A closeout may
cite them as **Tested (automated)** only after an exact passing run is recorded.

## Remaining verification and future-design gaps

The six source-review lanes are integrated and issue #280's release controls are reflected above,
but the following still require implementation or concrete verification evidence:

- Exact-head execution of every required deterministic, Windows, relay-staging, and CI check named
  above; none ran in this docs-only integration.
- Required GitHub environments/private-feed settings, the first signed publication, an authorized
  relay staging update/rollback, and #294/#270's desktop activation/state wiring.
- Semantic anti-cheat/evidence review for every future source surface; the lexical audit remains a
  partial ratchet rather than certification.
- Threat review of v2 components that do not yet exist or are scheduled for material redesign in
  `TBD_COMPONENTS.md`.
