# TBD: future components and material rebuilds

Every entry below was checked against current source. Some have no implementation; others are
material rebuilds of a current component whose future protocol or trust boundary has not been
chosen. Each row says which condition applies. They are named here, with what `ABUSE_CASES.md` and
`CONTROLS_AND_RESIDUAL_RISK.md` will owe once each lands, rather than modeled speculatively now.
Speculative modeling of a nonexistent or not-yet-designed flow would be exactly the kind of
unearned "reviewed" claim `METHODOLOGY.md`'s evidence tiers exist to prevent.

| Component | Issue | Checked in `src/` as | Threat-model debt once implemented |
| --- | --- | --- | --- |
| Local companion gateway (device pairing, relay transport, failover/fallback) | #304 | No `Gateway`-named project or directory anywhere in `src/`; current group transport explicitly permits local/LAN HTTP | A new trust boundary between the desktop and paired devices that is *not* the group relay — needs its own TB-N entry, its own actor (a paired device vs. a tablet holding a shared group key), and specific abuse cases for pairing-token theft, authenticated/confidential LAN transport, and failover (does falling back also degrade authentication?). Re-verify RISK-RELAY-KEY-DISCLOSURE and forbid silent fallback to cleartext. |
| Rebuilt relay operator administration, report lifecycle, and support-bundle redaction | #281, #310 | `AdminPanel.cs`, `RelayAdmin.cs`, `ProblemReports.cs`, `GroupMarks.cs`, and `SupportBundle.cs` exist and were reviewed in this pass, but #310 proposes a rebuild and #281 defines evidence-minimization obligations rather than the current design | RISK-RELAY-NO-RATE-LIMIT, RISK-RELAY-IDENTITY, RISK-RELAY-KEY-DISCLOSURE, RISK-RELAY-MARK-CAP, RISK-REPORT-RATE-LIMIT, RISK-REPORT-REDACTION, and TB-8/TB-9 abuse cases must be re-verified against the rebuilt design. #281/#310 must test a complete assembled bundle and persisted report for default absence of exact coordinates, full paths, screenshot filenames, game logs, and credentials; `Redact` alone is not sufficient evidence. If #310 introduces per-device/member identity, reassess RISK-GROUP-KEY-LOCAL-EXPOSURE instead of assuming identity alone solves credential storage. |
| Historical-traffic datasets and model snapshots (governance) | #311 | No dataset-governance or model-snapshot type found in `Core/Domain` | Directly gates boundary 8 in `ANTI_CHEAT_REVIEW.md` (honest modelled intelligence). Needs abuse cases for: a snapshot presented without its generation timestamp, a dataset whose sample coverage is misrepresented, and — the case #317 names explicitly — "model injection via catalog." Should also define what "model version" identity means concretely (a hash? a semantic version? tied to which training/aggregation run?) before this pass's boundary-8 verdict can move from "held as policy" to "held as enforced contract." |
| Health/character screen recognition | #305 | Current `HudProbe` recognizes whether the ordinary HUD/silhouette is visible; no inventory Health/Character screen target was found | A new evidence surface reading a screen that, per its own issue title, must not "invent state." Add cases for inference beyond visible player-owned evidence and deterministic assertions that it cannot express enemy state. Keep this separate from the existing coarse HUD-presence probe. |
| Screenshot cleanup and Debug Capture | #309 | Game-written screenshot cleanup already exists, defaults enabled at 24 hours, retains the newest, and uses the recycle bin. GDI capture bytes are never persisted. No current enableable Debug Capture persistence path was found | Reconcile RISK-SCREENSHOT-RETENTION-DEFAULT with explicit consent/upgrade behavior and visible effective state. Keep Debug Capture opt-in, bounded, redacted, and unmistakably separate from ordinary in-memory GDI capture; add retention/deletion tests without weakening `AGENTS.md`'s no-persistence default. |
| Shared 2D/2.5D/3D map scene and reviewed-content pipeline | #306 | No 3D/2.5D scene code found under `App/Views` or a map-scene project | A "reviewed-content pipeline" implies a new external-content trust boundary (who reviews it, what happens to unreviewed content) that does not exist for the current flat-tile map. Needs a TB-N entry once the pipeline's actual data flow is known. |
| `docs/V2_CONTRACT.md` (evidence/provenance envelope) | #264 | File does not exist on `main` | This is the formal contract `ANTI_CHEAT_REVIEW.md` boundary 8 depends on. Once it merges, re-review boundary 8 against the actual type definitions, specifically #264's own acceptance criterion "Predictions cannot be serialized with a live-detection evidence class" — check whether that is a compiler-enforced impossibility (e.g., a sealed type hierarchy) or a runtime validation, since those carry different residual risk. |
| Notifications, tray behavior, release UX, feedback | #314 | No tray/notification project found | Lower security relevance expected, but release UX changes should be checked against TB-10 (build/release trust boundary) if #314 changes how update prompts are presented — a notification is also a plausible phishing-style vector if it can be spoofed to request credentials or direct a user off the verified update channel. |
| Setup/Admin safety and methodology disclosure | #317, coordinated with #304/#310/#314 | The current Settings page contains the short statement "External and read-only toward the game" but no route to the complete safety/methodology material | Implement the presentation contract in `docs/security/README.md`: routine UI remains concise and layered, while Setup/Admin Data & Privacy exposes a clearly labelled bundled view or version-matched link to the full material. Routine surfaces must show active consent, outbound-data scope, pairing/authentication plus effective transport, active security/failure state, destructive consequences, and decision-changing uncertainty/provenance; the complete accepted/deferred register must be reachable there, not repeated in every workflow. Threat-review the link target/version binding so a released client cannot silently show documentation for a different build. |
| Workspace personalization, knowledge and export | #315 | Current source already has profile JSON import/export, quest-progress JSON exchange, and raid-history JSON/CSV export; #315 is a material expansion rather than the first export implementation | Re-review TB-11 and RISK-LOCAL-IMPORT-EXPORT-INTEGRITY for every new format. Specify data minimization, timestamp/freshness and replay rules, spreadsheet-safe CSV handling, provenance, user-selected destination behavior, and the `docs/SAFETY.md` prohibition on exporting/transmitting other players' log data. |
| Desktop placement and capture targeting | #316 | `Platform.Windows/Capture` and display/window discovery exist today; #316 proposes reliability work on top | Re-check boundaries 1 (memory access) and 7 (overlay) for every new window-handle/placement path. The current `SetWindowPos`/`WS_EX_TOPMOST` grep is only a two-token tripwire and does not prove an Avalonia/native placement feature cannot become an overlay. |

## What this means for #317's closeout, not just phase 1

`docs/security/` as a directory should be treated as a living register that grows one section per
component as each of the above lands, reviewed with the same evidence-tier discipline as this
pass — not re-derived from scratch each time. When a component here gets its first implementation,
the right move is to extend `SYSTEM_AND_TRUST_BOUNDARIES.md` with a new `TB-N`, add its abuse
cases to `ABUSE_CASES.md`, and add its findings to `CONTROLS_AND_RESIDUAL_RISK.md` — not to start a
new document set.
